<?xml version="1.0"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Peter's Playground</title>
    <link>http://peters-playground.com/</link>
    <atom:link href="http://peters-playground.com/rss.xml" rel="self" type="application/rss+xml" />
    <description>关于前端方面的资讯分享，经验总结，与深度思考</description>
    <language>zh_CN</language>
    <pubDate>Thu, 14 Mar 2013 00:30:21 +0000</pubDate>
    <lastBuildDate>Thu, 14 Mar 2013 00:30:21 +0000</lastBuildDate>

    
    <item>
      <title>【分享】Blankwork 最棒的网格系统</title>
      <link>http://peters-playground.com/Css/2013/03/13/blankwork.html</link>
      <pubDate>13 Mar 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Css/2013/03/13/blankwork</guid>
      <description>&lt;p&gt;最近有点忙，没有坚持写博客很不应该，今天与大家分享一款 SCSS Grid System —— Blankwork，这是我所有了解过的网格系统中最牛也是最好用的一款，与其他相比不需要定义任何一个额外的 class，通过调用自定义函数与 maxin 直接给元素编译样式，所以强烈推荐所有 Sass 用户尝试一下。地址：&lt;a href=&#39;http://www.blankwork.net/&#39;&gt;http://www.blankwork.net/&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;首先，导入 &lt;code&gt;blankwork.sass&lt;/code&gt; 文件，这个文件中有几个参数可以修改，默认宽度 960px，12列布局，间距 20px。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$_columns: 12 !default;      // Total number of columns
$_column-width: 60px !default;   // Width of a single column
$_gutter: 20px !default;     // Width of the gutter&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;导入后就可以在主文件中调用 Blankwork 的 function 与 mixin 了，主要包含 5 个功能，分别是 &lt;code&gt;wrapper&lt;/code&gt;, &lt;code&gt;container&lt;/code&gt;, &lt;code&gt;column&lt;/code&gt;, &lt;code&gt;get_width&lt;/code&gt;, 以及 &lt;code&gt;clearfix&lt;/code&gt;。接下来我会一一介绍，同时我会使用 &lt;code&gt;@includ&lt;/code&gt; 的简写也就是 &lt;code&gt;+&lt;/code&gt; (&lt;code&gt;@mixin&lt;/code&gt; 的简写是 &lt;code&gt;=&lt;/code&gt;)。&lt;/p&gt;

&lt;h4 id=&#39;1_&#39;&gt;1. &lt;code&gt;+wrapper($columns, $center, $subtract)&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;Wrapper 定义最外层的宽度，有三个参数可用: 列数（默认：12），居中（默认：true），偏移量（默认 0）。以默认值为例：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#demo
	+wrapper&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;输出&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#demo {
  width: 960px;
  margin-left: auto;
  margin-right: auto;
  *zoom: 1; }
  #demo:before, #demo:after {
    content: &amp;quot;&amp;quot;;
    display: table; }
  #demo:after {
    clear: both; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;可以看到输出样式是 12 列的总宽度也就是 960px，居中效果，并且清除浮动。这里主要用到了前两个参数，第三个参数“偏移量”是什么意思呢？如果接触过 960 或者其他网格系统，一定知道使用 &lt;code&gt;border&lt;/code&gt; 与 &lt;code&gt;padding&lt;/code&gt; 时需要额外的处理，很麻烦，“偏移量”参数就是为了简化这个步骤设计的。例如添加 1px 的边框，需要在总宽度上减去 2px ：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#demo
	+wrapper(12,true,2)
	border: 1px solid black&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;或者:&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#demo
	+wrapper(12,true,22)
	padding 10px
	border: 1px solid black&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;输出宽度是 960px 减去“偏移量”，算上 &lt;code&gt;padding&lt;/code&gt; 与 &lt;code&gt;border&lt;/code&gt; 总宽度还是 960px。另外，建议在 &lt;code&gt;blankwork.sass&lt;/code&gt; 文件中调整一下偏移量与居中的顺序，不然要多打一次 true。&lt;/p&gt;

&lt;h4 id=&#39;2_&#39;&gt;2. &lt;code&gt;+container($columns, $subtract)&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;Container 使用两个参数：列数（默认：12），偏移量（默认：0），作用是允许嵌套列。最外层 wrapper 中需要继续向下嵌套列时，就要用这个 mixin 了，而且使用 container 就不需要再使用 &lt;code&gt;alpha&lt;/code&gt;, &lt;code&gt;omega&lt;/code&gt; 了，超级方便！编译效果如下：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#demo
	+container(12)&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;输出：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#demo {
  display: inline;
  float: left;
  width: 960px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h4 id=&#39;3_&#39;&gt;3. &lt;code&gt;column($columns, $subtract, $margin-override)&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;Column 使用三个参数：列数（默认：12），偏移量（默认：0），间距（默认：20px，每侧各 10px）。前两个相信不用介绍了，第三个参数调左右的 &lt;code&gt;margin&lt;/code&gt; 值，与 960 的 &lt;code&gt;prefix&lt;/code&gt; &lt;code&gt;suffix&lt;/code&gt; 单向 &lt;code&gt;padding&lt;/code&gt; 不同，参数值除以二平均分配给两侧，相信不会经常用到。&lt;/p&gt;

&lt;h4 id=&#39;4_&#39;&gt;4. &lt;code&gt;get_width($columns, $onlyInnerWidth)&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;Get_width 是个自定义函数，用于提取宽度给其他属性使用。第一个参数设定列数（默认：12），第二参数决定提取的宽度是否包含间距，默认：true，也就是不包含。用法如下：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#demo
  width: get_width(3)
	marging-left: get_width(3,false)&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h4 id=&#39;5_&#39;&gt;5. &lt;code&gt;clearfix&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;清除浮动： &lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#demo
  +clearfix&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;/p&gt;

&lt;p&gt;使用 blankwork 的这段时间省心省力，确实非常不错，如果以后支持响应式设计就更完美了！&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>【分享】Sass for Sublime Text 2</title>
      <link>http://peters-playground.com/Css/2013/03/02/sass-syntax.html</link>
      <pubDate>02 Mar 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Css/2013/03/02/sass-syntax</guid>
      <description>&lt;p&gt;用过几个版本的 Sass 代码高亮插件，效果都不太理想，所以参考 &lt;a href=&#39;https://github.com/nathos/sass-textmate-bundle&#39;&gt;nathos&amp;#8217;s sass-textmate-bundle&lt;/a&gt; 自己也写了一个，简单列出新加入的特色功能，欢迎大家试用。&lt;/p&gt;

&lt;ol&gt;
&lt;li&gt;采用匹配语句结构的方式代替匹配关键词，因此不会出现某些词无法高亮的情况，但有些根据 Scope Selector 的 snippets 可能会失效；&lt;/li&gt;

&lt;li&gt;支持属性选择器 (Attribute Selector) 高亮；&lt;/li&gt;

&lt;li&gt;支持 Sass 专有语句，包括 Variable, Directive, Directive Shorthand (&lt;code&gt;=&lt;/code&gt; &lt;code&gt;+&lt;/code&gt;), Placeholder Selector, Build-in Function, Interpolation 等等；&lt;/li&gt;

&lt;li&gt;所有 &lt;code&gt;something(…)&lt;/code&gt; 将当做 function 结构处理，包括 &lt;code&gt;url()&lt;/code&gt;, &lt;code&gt;format()&lt;/code&gt;, 部分 CSS 属性值，Sass 自带函数，Compass/Bourbon 函数，自定义函数等等（注：函数的左括号在 Sublime Text 中无法更改颜色，Textmate 中可以，已经在官方论坛询问，可惜暂时还没有解决办法）；&lt;/li&gt;

&lt;li&gt;Sass 的 at-rule 当做 directive 结构处理，以后会进一步优化这部分的高亮效果；&lt;/li&gt;

&lt;li&gt;优化了 &lt;code&gt;@media&lt;/code&gt; 高亮效果； 7. 支持运算符高亮；&lt;/li&gt;

&lt;li&gt;输入 id，class，placeholder 选择器时禁用 snippets；&lt;/li&gt;

&lt;li&gt;分别支持 SCSS 与 Sass 两种语句结构，而不是将其合并在一起。&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;更多信息请查看 Github：&lt;a href=&#39;https://github.com/P233/Syntax-highlighting-for-Sass&#39; target=&#39;_blank&#39;&gt;https://github.com/P233/Syntax-highlighting-for-Sass&lt;/a&gt;，意见反馈欢迎联系我，大家一起交流！！&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>【翻译】牢记 30 种 CSS 选择器用法</title>
      <link>http://peters-playground.com/Css/2013/01/31/css-selectors-must-memorize.html</link>
      <pubDate>31 Jan 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Css/2013/01/31/css-selectors-must-memorize</guid>
      <description>&lt;p&gt;原文 &lt;a href=&#39;http://net.tutsplus.com/tutorials/html-css-techniques/the-30-css-selectors-you-must-memorize/&#39; target=&#39;_blank&#39;&gt;The 30 CSS Selectors you Must Memorize&lt;/a&gt; 由 Jeffrey Way 发表于 2011 年 6 月，介绍了 30 种最常用的 CSS 选择器用法，其他 CSS3 选择器用法请查看 &lt;a href=&#39;http://www.456bereastreet.com/archive/200601/css_3_selectors_explained/&#39; target=&#39;_blank&#39;&gt;CSS3 selectors explained&lt;/a&gt; 这篇文章，此外，W3C CSS3 选择器官方文档 &lt;a href=&#39;http://www.w3.org/TR/css3-selectors/&#39; target=&#39;_blank&#39;&gt;Selectors Level 3 W3C Recommendation 29 September 2011&lt;/a&gt; 记录在这里留待有疑问的时候查看。&lt;/p&gt;

&lt;h3 id=&#39;1_&#39;&gt;1. &lt;code&gt;*&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;* {  
   margin: 0;  
   padding: 0;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;星号选择器用于选取页面中的所有元素，可用于快速清除所有元素的 &lt;code&gt;margin&lt;/code&gt; 与 &lt;code&gt;padding&lt;/code&gt;，但最好只在测试的时候使用，而不要正式用在 CSS 文件中，否则会大大加重浏览器负担。此外，星号选择器也可以给父层的所有子元素设定样式，重复一遍，尽量少用这种方式：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#container * {  
   border: 1px solid black;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;兼容 IE6+&lt;/p&gt;

&lt;h3 id=&#39;2_&#39;&gt;2. &lt;code&gt;#X&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#container {  
   width: 960px;  
   margin: auto;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;id 选择器，最常见的选择器用法之一，不可重复使用。&lt;/p&gt;

&lt;p&gt;兼容 IE6+&lt;/p&gt;

&lt;h3 id=&#39;3_&#39;&gt;3. &lt;code&gt;.X&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error {  
  color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;class 选择器，也是最常见的选择器用法之一，与 id 选择器不同的是 class 选择器可同时选取多个元素，而 id 选择器只能给一个独一无二的元素设定样式。&lt;/p&gt;

&lt;p&gt;兼容 IE6+&lt;/p&gt;

&lt;h3 id=&#39;4_&#39;&gt;4. &lt;code&gt;X Y&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;li a {  
  text-decoration: none;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;后代选择器 (descendant selector)，选取 X 元素内的所有 Y 元素，比如上面这段代码将选取 li 标签内的所有链接。&lt;/p&gt;

&lt;p&gt;兼容 IE6+&lt;/p&gt;

&lt;h3 id=&#39;5_&#39;&gt;5. &lt;code&gt;X&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a { color: red; }  
ul { margin-left: 0; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;标签选择器 (type selector)，用于选取 HTML 标签 (tag)。&lt;/p&gt;

&lt;p&gt;兼容 IE6+&lt;/p&gt;

&lt;h3 id=&#39;6__and_&#39;&gt;6. &lt;code&gt;X:visited&lt;/code&gt; and &lt;code&gt;X:link&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a:link { color: red; }  
a:visted { color: purple; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;:link&lt;/code&gt; 伪类选择器 (pseudo class selector) 用于选取所有&lt;em&gt;未点击过&lt;/em&gt;的链接，而 &lt;code&gt;:visited&lt;/code&gt; 则用于选取所有&lt;em&gt;已访问过&lt;/em&gt;的链接。&lt;/p&gt;

&lt;p&gt;兼容 IE6+&lt;/p&gt;

&lt;h3 id=&#39;7_&#39;&gt;7. &lt;code&gt;X + Y&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;ul + p {  
   color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;临近选择器 (adjacent selector)，选取紧邻在 X 元素后面出现的第一个元素，比如上面这段代码将选取 ul 元素后出现的第一个元素，也就是 p 元素。&lt;/p&gt;

&lt;p&gt;兼容性 IE6+&lt;/p&gt;

&lt;h3 id=&#39;8_&#39;&gt;8. &lt;code&gt;X &amp;gt; Y&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;div#container &amp;gt; ul {  
  border: 1px solid black;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;在第 4 条中，后代选择器 &lt;code&gt;X Y&lt;/code&gt; 选取父层 X 内的所有 Y 元素；子选择器 &lt;code&gt;X &amp;gt; Y&lt;/code&gt; 则只选取直接出现在父层 X 内的 Y 元素。比如下面的 HTML 结构中，&lt;code&gt;#container &amp;gt; ul&lt;/code&gt; 选取直接出现在 &lt;code&gt;div#container&lt;/code&gt; 内的 ul 元素，不包含嵌套在 li 内的 ul 元素：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;&amp;lt;div id=&amp;quot;container&amp;quot;&amp;gt;  
   &amp;lt;ul&amp;gt;  
      &amp;lt;li&amp;gt; List Item  
        &amp;lt;ul&amp;gt;  
           &amp;lt;li&amp;gt; Child &amp;lt;/li&amp;gt;  
        &amp;lt;/ul&amp;gt;  
      &amp;lt;/li&amp;gt;  
      &amp;lt;li&amp;gt; List Item &amp;lt;/li&amp;gt;  
      &amp;lt;li&amp;gt; List Item &amp;lt;/li&amp;gt;  
      &amp;lt;li&amp;gt; List Item &amp;lt;/li&amp;gt;  
   &amp;lt;/ul&amp;gt;  
&amp;lt;/div&amp;gt;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;兼容 IE6+&lt;/p&gt;

&lt;h3 id=&#39;9_&#39;&gt;9. &lt;code&gt;X ~ Y&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;ul ~ p {  
   color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;同样也是临近选择器，前面第 7 条 &lt;code&gt;X + Y&lt;/code&gt; 选取紧邻在 X 后出现的第一个元素，而 &lt;code&gt;X ~ Y&lt;/code&gt; 将选取 X 元素后出现的所有同级元素。上面这段代码将选取 ul 元素后出现的所有同级 p 元素，而不是像 &lt;code&gt;ul + p&lt;/code&gt; 这样选取第一个出现的 p 元素。&lt;/p&gt;

&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;10_&#39;&gt;10. &lt;code&gt;X[title]&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a[title] {  
   color: green;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;属性选择器 (attributes selector)，根据元素使用的属性进一步缩小选取范围，上面这段代码将选取所有使用了 &lt;code&gt;title&lt;/code&gt; 属性的链接，或者 &lt;code&gt;a[title=&amp;quot;title content&amp;quot;]{color:green}&lt;/code&gt; 再进一步缩小选取范围。&lt;/p&gt;

&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;11_&#39;&gt;11. &lt;code&gt;X[href=&amp;quot;foo&amp;quot;]&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a[href=&amp;quot;http://net.tutsplus.com&amp;quot;] {  
  color: #1f6053; /* nettuts green */
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;上面这段代码将选取所有跳转到 &lt;code&gt;http://net.tutsplus.com&lt;/code&gt; 的链接，这些链接将显示为绿色，其他链接不受影响。&lt;/p&gt;

&lt;p&gt;只是这种方式很严格不能相差一个字符，下面将会逐一介绍更灵活的用法。&lt;/p&gt;

&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;12_&#39;&gt;12. &lt;code&gt;X[href*=&amp;quot;nettuts&amp;quot;]&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a[href*=&amp;quot;tuts&amp;quot;] {  
  color: #1f6053; /* nettuts green */  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;*&lt;/code&gt; 表示只要属性值中&lt;em&gt;包含&lt;/em&gt;双引号内的内容就满足选取要求，这段代码将选取跳转到 &lt;code&gt;nettuts.com&lt;/code&gt;，&lt;code&gt;net.tutsplus.com&lt;/code&gt;，或者 &lt;code&gt;tutsplus.com&lt;/code&gt; 等链接。&lt;/p&gt;

&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;13_&#39;&gt;13. &lt;code&gt;X[href^=&amp;quot;http&amp;quot;]&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a[href^=&amp;quot;http&amp;quot;] {
   background: url(path/to/external/icon.png) no-repeat;  
   padding-left: 10px;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;^&lt;/code&gt; 表示只要属性值以双引号内的内容&lt;em&gt;开头&lt;/em&gt;就满足选取要求，这段代码也常用来给页面中所有外部接设定样式。&lt;/p&gt;

&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;14_&#39;&gt;14. &lt;code&gt;X[href$=&amp;quot;.jpg&amp;quot;]&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a[href$=&amp;quot;.jpg&amp;quot;] {  
   color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;$&lt;/code&gt; 表示只要属性值以双引号内的内容&lt;em&gt;结尾&lt;/em&gt;就满足选取要求，这段代码将选取所有跳转到 jpg 图片的链接。&lt;/p&gt;

&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;15_&#39;&gt;15. &lt;code&gt;X[data-*=&amp;quot;foo&amp;quot;]&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;上面第 14 条提到了如何选取所有跳转到 jpg 图片的链接，若要选取跳转到图片的所有链接可以用下面的方法：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a[href$=&amp;quot;.jpg&amp;quot;],  
a[href$=&amp;quot;.jpeg&amp;quot;],  
a[href$=&amp;quot;.png&amp;quot;],  
a[href$=&amp;quot;.gif&amp;quot;] {  
   color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;或者，先给图片链接添加 &lt;code&gt;data-&lt;/code&gt; 属性（注：&lt;a href=&#39;http://html5doctor.com/html5-custom-data-attributes/&#39; target=&#39;_blank&#39;&gt;HTML5 Custom Data Attributes&lt;/a&gt;）&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;&amp;lt;a href=&amp;quot;path/to/image.jpg&amp;quot; data-filetype=&amp;quot;image&amp;quot;&amp;gt; Image Link &amp;lt;/a&amp;gt;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;然后再通过属性选择器选取：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a[data-filetype=&amp;quot;image&amp;quot;] {  
   color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;16_&#39;&gt;16. &lt;code&gt;X[foo~=&amp;quot;bar&amp;quot;]&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a[data-info~=&amp;quot;external&amp;quot;] {  
   color: red;  
}  
a[data-info~=&amp;quot;image&amp;quot;] {  
   border: 1px solid black;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;如果属性值中有用空格分隔的一连串属性值，&lt;code&gt;~&lt;/code&gt; 可以选取其中一个属性值，比如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;&amp;quot;&amp;lt;a href=&amp;quot;path/to/image.jpg&amp;quot; data-info=&amp;quot;external image&amp;quot;&amp;gt; Click Me, Fool &amp;lt;/a&amp;gt;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;借助 &lt;code&gt;~&lt;/code&gt; 选取包含 &lt;code&gt;external&lt;/code&gt; 或者 &lt;code&gt;image&lt;/code&gt; 属性值的元素：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;/* Target data-info attr that contains the value &amp;quot;external&amp;quot; */  
a[data-info~=&amp;quot;external&amp;quot;] {  
   color: red;  
}  
/* And which contain the value &amp;quot;image&amp;quot; */  
a[data-info~=&amp;quot;image&amp;quot;] {  
  border: 1px solid black;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;17_&#39;&gt;17. &lt;code&gt;X:checked&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;input[type=radio]:checked {  
   border: 1px solid black;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;:checked&lt;/code&gt; 伪类选择器用于选取所有标记为 &lt;em&gt;checked&lt;/em&gt; 的元素，比如单选框 (radio button) 或复选框 (checkbox)。&lt;/p&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;18_&#39;&gt;18. &lt;code&gt;X:after&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;&lt;code&gt;:before&lt;/code&gt; 与 &lt;code&gt;:after&lt;/code&gt; 是两个令人兴奋的伪类选择器，几乎每天都有人发明出一些新用法，这里简单介绍一下如何用它清除浮动：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.clearfix:after {  
    content: &amp;quot;&amp;quot;;  
    display: block;  
    clear: both;  
    visibility: hidden;  
    font-size: 0;  
    height: 0;  
    }  
.clearfix {  
   *display: inline-block;  
   _height: 1%;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;这种方式通过 &lt;code&gt;:after&lt;/code&gt; 在元素后面添加一块区域，然后将其隐藏，可以弥补 &lt;code&gt;overflow: hidden;&lt;/code&gt; 的缺陷。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;根据 CSS3 选择器标准，理论上伪类选择器应该使用双冒号，但实际上浏览器也支持单冒号的形式，所以可以继续使用单冒号。&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;兼容 IE8+&lt;/p&gt;

&lt;h3 id=&#39;19_&#39;&gt;19. &lt;code&gt;X:hover&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;div:hover {  
  background: #e3e3e3;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;最常用的伪类选择器，不多解释了，只是需要注意 IE6 不支持将 &lt;code&gt;:hover&lt;/code&gt; 作用于除 &lt;code&gt;a&lt;/code&gt; 链接外的其他元素。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a:hover {  
 border-bottom: 1px solid black;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;另外提醒一点：&lt;code&gt;border-bottom: 1px solid black;&lt;/code&gt; 的效果要比 &lt;code&gt;text-decoration: underline;&lt;/code&gt; 好看一些。&lt;/p&gt;

&lt;p&gt;兼容 IE6+ （在 IE6 中 &lt;code&gt;:hover&lt;/code&gt; 只能作用于链接）&lt;/p&gt;

&lt;h3 id=&#39;20_&#39;&gt;20. &lt;code&gt;X:not(selector)&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;div:not(#container) {  
   color: blue;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;:not&lt;/code&gt; 伪类选择器有时会起到很重要的作用，假设现在要选取除 &lt;code&gt;#contaienr&lt;/code&gt; 外的所有 &lt;code&gt;div&lt;/code&gt; 元素，就可以用上面的代码实现。&lt;/p&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;21_&#39;&gt;21. &lt;code&gt;X::pseudoElement&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p::first-line {  
   font-weight: bold;  
   font-size: 1.2em;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;通过伪元素（使用双冒号 &lt;code&gt;::&lt;/code&gt;）可以给元素的某一部分设定样式，比如第一行、或者第一个字母。需要注意的是，这只对块级元素 (block level elements) 生效。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;提示：伪元素 (pseudo element) 使用双冒号 &lt;code&gt;::&lt;/code&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;h4 id=&#39;id1190&#39;&gt;选取段落的第一个字母&lt;/h4&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p::first-letter {  
   float: left;  
   font-size: 2em;  
   font-weight: bold;  
   font-family: cursive;  
   padding-right: 2px;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;这段代码将选取页面中所有 p 元素，然后再选取其中的第一个字母。&lt;/p&gt;

&lt;h4 id=&#39;id1191&#39;&gt;选取段落的第一行&lt;/h4&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p::first-line {  
   font-weight: bold;  
   font-size: 1.2em;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;与上面的例子类似，通过 &lt;code&gt;::first-line&lt;/code&gt; 选取页面的第一行。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;为了兼容 CSS1 与 CSS2 中的伪类选择器（比如 &lt;code&gt;:first-line&lt;/code&gt;, &lt;code&gt;:first-letter&lt;/code&gt;, &lt;code&gt;:before&lt;/code&gt; 以及 &lt;code&gt;:after&lt;/code&gt;），浏览器接受单冒号与双冒号两种格式，但对于 CSS3 中最新引入的伪元素，必须使用双冒号。&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;兼容 IE6+&lt;/p&gt;

&lt;h3 id=&#39;22_&#39;&gt;22. &lt;code&gt;X:nth-child(n)&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;li:nth-child(3) {  
   color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;:nth-child(n)&lt;/code&gt; 用于选取 stack 中的某一个元素，只接受整数作参数（参数从 1 开始计数），如果你想选取第二个 li 元素，只需这样写 &lt;code&gt;li:nth-child(2)&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;也可以设定可变的参数，比如 &lt;code&gt;li:nth-child(4n)&lt;/code&gt; 将选取第 4, 8 , 12… 个元素（4*n, n=1, n++）。&lt;/p&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;23_&#39;&gt;23. &lt;code&gt;X:nth-last-child(n)&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;li:nth-last-child(2) {  
   color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;除了正序（从上往下）选择，也可以使用 &lt;code&gt;:nth-last-child(n)&lt;/code&gt; 倒序（从下往上）选择第几个元素，其他用法与第 22 条完全一样。&lt;/p&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;24_&#39;&gt;24. &lt;code&gt;X:nth-of-type(n)&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;ul:nth-of-type(3) {  
   border: 1px solid black;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;:nth-of-type(n)&lt;/code&gt; 的作用不是选取子元素 (child element)，而是选取同类元素 (type of element)。想象一下 HTML 文件中包含 5 个 ul 元素，现在要选取第三个，只需使用上面的代码，而不用再单独这个 ul 添加 &lt;code&gt;id&lt;/code&gt;。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;关于 &lt;code&gt;:nth-child&lt;/code&gt; 与 &lt;code&gt;:nth-of-type&lt;/code&gt; 的区别，具体请查看 &lt;a href=&#39;http://css-tricks.com/the-difference-between-nth-child-and-nth-of-type/&#39; target=&#39;_blank&#39;&gt;CSS-Tricks 网站的解释&lt;/a&gt;，简单来说，如果父层内只包含一种元素（比如都是 p 元素）那两种用法是等效的，如果父层包含多种元素（比如 p 元素与同级的其他元素），需要选取第几个 p 元素时应该用 &lt;code&gt;:nth-of-type&lt;/code&gt;。&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;25_&#39;&gt;25. &lt;code&gt;X:nth-last-of-type(n)&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;ul:nth-last-of-type(3) {  
   border: 1px solid black;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;与第 24 条用法相同，倒序选取同类元素。&lt;/p&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;26_&#39;&gt;26. &lt;code&gt;X:first-child&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;ul li:first-child {  
   border-top: none;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;选取父层内的第一个子元素。&lt;/p&gt;

&lt;p&gt;兼容 IE7+&lt;/p&gt;

&lt;h3 id=&#39;27_&#39;&gt;27. &lt;code&gt;X:last-child&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;ul &amp;gt; li:last-child {  
   color: green;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;与第 26 条用法相同，区别在于 &lt;code&gt;:last-child&lt;/code&gt; 选取父层元素内的最后一个子元素。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;:first-child&lt;/code&gt; 与 &lt;code&gt;:last-child&lt;/code&gt; 通常用来清除边框 (border)，比如 &lt;code&gt;&amp;lt;ul&amp;gt;&amp;lt;/ul&amp;gt;&lt;/code&gt; 内每个 &lt;code&gt;&amp;lt;li&amp;gt;&amp;lt;/li&amp;gt;&lt;/code&gt; 都使用了 &lt;code&gt;border-top&lt;/code&gt; 与 &lt;code&gt;border-bottom&lt;/code&gt; 边框，结果是，第一个元素的上方与最后一个元素的下方会是单边框效果。这种情况可以用 &lt;code&gt;:first-child&lt;/code&gt; 与 &lt;code&gt;:last-child&lt;/code&gt; 清除上下的边框，而不用给第一个元素添加 &lt;code&gt;id=&amp;quot;first&amp;quot;&lt;/code&gt; 或者给最后一个元素添加 &lt;code&gt;id=&amp;quot;last&amp;quot;&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;28_&#39;&gt;28. &lt;code&gt;X:only-child&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;div p:only-child {  
   color: red;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;这个伪类选择器不常用，它可以选取包含唯一指定子元素的父层。比如上面的代码中将选取下面第一个 div 元素，而不是第二个 div 中的 p 元素。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;&amp;lt;div&amp;gt;&amp;lt;p&amp;gt; My paragraph here. &amp;lt;/p&amp;gt;&amp;lt;/div&amp;gt;  
&amp;lt;div&amp;gt;  
   &amp;lt;p&amp;gt; Two paragraphs total. &amp;lt;/p&amp;gt;  
   &amp;lt;p&amp;gt; Two paragraphs total. &amp;lt;/p&amp;gt;  
&amp;lt;/div&amp;gt;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;29_&#39;&gt;29. &lt;code&gt;X:only-of-type&lt;/code&gt;&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;li:only-of-type {  
   font-weight: bold;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;这个选择器会选取某个元素，并且这个元素在其父层内没有其他同级同类元素（不一定是唯一元素）。比如，要选取所有只包含一个 li 元素的 ul 元素该怎么做呢？如果使用 &lt;code&gt;ul li&lt;/code&gt; 将选取所有 li 元素，应该使用 &lt;code&gt;only-of-type&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;30_&#39;&gt;30. &lt;code&gt;X:first-of-type&lt;/code&gt;&lt;/h3&gt;

&lt;p&gt;&lt;code&gt;first-of-type&lt;/code&gt; 伪类可以选取某种元素的第一个同类元素。&lt;/p&gt;

&lt;p&gt;为了更好地理解它的用法，现在思考一下如何在下面的 HTML 结构中选取 &lt;code&gt;List Item 2&lt;/code&gt; ？&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;&amp;lt;div&amp;gt;  
   &amp;lt;p&amp;gt; My paragraph here. &amp;lt;/p&amp;gt;  
   &amp;lt;ul&amp;gt;  
      &amp;lt;li&amp;gt; List Item 1 &amp;lt;/li&amp;gt;  
      &amp;lt;li&amp;gt; List Item 2 &amp;lt;/li&amp;gt;  
   &amp;lt;/ul&amp;gt;  
   &amp;lt;ul&amp;gt;  
      &amp;lt;li&amp;gt; List Item 3 &amp;lt;/li&amp;gt;  
      &amp;lt;li&amp;gt; List Item 4 &amp;lt;/li&amp;gt;  
   &amp;lt;/ul&amp;gt;  
&amp;lt;/div&amp;gt;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h4 id=&#39;id1192&#39;&gt;方法一&lt;/h4&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;ul:first-of-type &amp;gt; li:nth-child(2) {  
   font-weight: bold;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;这段代码的意思是：首先选取第一个 ul 元素；然后选取其中的所有直接子元素，也就是 li；最后选取第二个子元素。&lt;/p&gt;

&lt;h4 id=&#39;id1193&#39;&gt;方法二&lt;/h4&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p + ul li:last-child {  
   font-weight: bold;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;找到 p 元素后第一个出现的 ul 元素，然后选取其中的最后一个子元素。&lt;/p&gt;

&lt;h4 id=&#39;id1194&#39;&gt;方法三&lt;/h4&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;ul:first-of-type li:nth-last-child(1) {  
   font-weight: bold;  
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;找到第一个 ul 元素，然后从上往下选取第一个子元素。&lt;/p&gt;

&lt;p&gt;兼容 IE9+&lt;/p&gt;

&lt;h3 id=&#39;31_&#39;&gt;31. 伪类选择器联用&lt;/h3&gt;

&lt;p&gt;偶然发现有些伪类选择器是可以联用的，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#post p:nth-of-type(2):first-letter {
   float: left;
   margin: 0 5px 0 1em;
   width: 1em;
   height: 1em;
   font-size: 2em;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;兼容 IE9+&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>【翻译】Sass 官方文档中文版</title>
      <link>http://peters-playground.com/Css/2013/01/24/sass-reference.html</link>
      <pubDate>24 Jan 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Css/2013/01/24/sass-reference</guid>
      <description>&lt;p&gt;使用 Sass 已经有几个月的时间了，翻来覆去还是在用几个简单的功能，下决心好好学习一下，所以翻译了官方文档。翻译的过程也是学习与积累的过程，深入理解了几个复杂功能，同时也补充了一些基础知识。只是官方文档写得实在绕口，花了很久才翻译完，有几处地方不太理解，翻译不出来，还请大家帮忙，理解错误的地方也请大家指正。关于 Sass 设置的部分暂时未翻译，因为只会用 &lt;a href=&#39;http://incident57.com/codekit/&#39; target=&#39;_blank&#39;&gt;Codekit&lt;/a&gt;，对这些没有概念。如果哪位愿意代为翻译，或者交流，&lt;a href=&#39;mailto:40132147&amp;#64;&amp;#113;&amp;#113;&amp;#46;&amp;#99;&amp;#111;&amp;#109;?Subject=Hi%20Peter&#39; target=&#39;_blank&#39;&gt;请联系 Peter&lt;/a&gt;。&lt;/p&gt;

&lt;p&gt;Sass 官方文档，&lt;a href=&#39;http://sass-lang.com/docs/yardoc/file.SASS_REFERENCE.html&#39; target=&#39;_blank&#39;&gt;原地址在请看这里&lt;/a&gt;，相当于 Sass 的功能概览，要学好 Sass 还是要多方面学习，&lt;a href=&#39;http://manning.com/netherland/&#39; target=&#39;_blank&#39;&gt;《Sass and Compass in action》&lt;/a&gt; 这本书下个月底出版，非常期待。&lt;/p&gt;
&lt;div id=&#39;contents&#39;&gt;
&lt;h3&gt;目录&lt;/h3&gt;
&lt;a href=&#39;#1&#39;&gt;1. 特色功能 (Features)&lt;/a&gt;
&lt;a href=&#39;#2&#39;&gt;2. 语言格式 (Syntax)&lt;/a&gt;
&lt;a href=&#39;#3&#39;&gt;3. 使用 Sass (Using Sass)&lt;/a&gt;
&lt;!--
&lt;a href=&quot;#3_1&quot;&gt;3.1. Rack/Rails/Merb Plugins&lt;/a&gt;
&lt;a href=&quot;#3_2&quot;&gt;3.2. &lt;/a&gt;
&lt;a href=&quot;#3_3&quot;&gt;3.3. &lt;/a&gt;
&lt;a href=&quot;#3_4&quot;&gt;3.4. &lt;/a&gt;
&lt;a href=&quot;#3_5&quot;&gt;3.5. &lt;/a&gt;
&lt;a href=&quot;#3_5_1&quot;&gt;3.5.1 &lt;/a&gt;
--&gt;
&lt;a href=&#39;#4&#39;&gt;4. CSS 拓展 (CSS Extensions)&lt;/a&gt;
&lt;div class=&#39;level2&#39;&gt;
&lt;a href=&#39;#4_1&#39;&gt;4.1. 嵌套规则 (Nested Rules)&lt;/a&gt;
&lt;a href=&#39;#4_2&#39;&gt;4.2. 引用父选择器 &amp;amp; (Referencing Parent Selectors: &amp;amp;)&lt;/a&gt;
&lt;a href=&#39;#4_3&#39;&gt;4.3. 属性嵌套 (Nested Properties)&lt;/a&gt;
&lt;a href=&#39;#4_4&#39;&gt;4.4. 占位符选择器 %foo (Placeholder Selectors: %foo)&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#5&#39;&gt;5. 注释 /* */ 与 // (Comments: /* */ and //)&lt;/a&gt;
&lt;a href=&#39;#6&#39;&gt;6. SassScript&lt;/a&gt;
&lt;div class=&#39;level2&#39;&gt;
&lt;a href=&#39;#6_1&#39;&gt;6.1. Interactive Shell&lt;/a&gt;
&lt;a href=&#39;#6_2&#39;&gt;6.2. 变量 $ (Variables: $)&lt;/a&gt;
&lt;a href=&#39;#6_3&#39;&gt;6.3. 数据类型 (Data Types)&lt;/a&gt;
&lt;div class=&#39;level3&#39;&gt;
&lt;a href=&#39;#6_3_1&#39;&gt;6.3.1. 字符串 (Strings)&lt;/a&gt;
&lt;a href=&#39;#6_3_2&#39;&gt;6.3.2. 值列表 (Lists)&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#6_4&#39;&gt;6.4. 运算符 (Operations)&lt;/a&gt;
&lt;div class=&#39;level3&#39;&gt;
&lt;a href=&#39;#6_4_1&#39;&gt;6.4.1. 数字运算 (Number Operations)&lt;/a&gt;
&lt;a class=&#39;level4&#39; href=&#39;#6_4_1_1&#39;&gt;6.4.1.1. 除法运算 / (Division and /)&lt;/a&gt;
&lt;a href=&#39;#6_4_2&#39;&gt;6.4.2. 颜色运算 (Color Operations)&lt;/a&gt;
&lt;a href=&#39;#6_4_3&#39;&gt;6.4.3. 字符串运算 (String Operations)&lt;/a&gt;
&lt;a href=&#39;#6_4_4&#39;&gt;6.4.4. 布尔运算 (Boolean Operations)&lt;/a&gt;
&lt;a href=&#39;#6_4_5&#39;&gt;6.4.5. 值列表运算 (List Operations)&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#6_5&#39;&gt;6.5. 圆括号 (Parentheses)&lt;/a&gt;
&lt;a href=&#39;#6_6&#39;&gt;6.6. 函数 (Functions)&lt;/a&gt;
&lt;div class=&#39;level3&#39;&gt;
&lt;a href=&#39;#6_6_1&#39;&gt;6.6.1. 关键词参数 (Keyword Arguments)&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#6_7&#39;&gt;6.7. 插值语句 #{ } (Interpolation: #{ })&lt;/a&gt;
&lt;a href=&#39;#6_8&#39;&gt;6.8. 变量定义 !default (Variable Defaults: !default)&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#7&#39;&gt;7. @-Rules 与指令 (@-Rules and Directives)&lt;/a&gt;
&lt;div class=&#39;level2&#39;&gt;
&lt;a href=&#39;#7_1&#39;&gt;7.1. @import&lt;/a&gt;
&lt;div class=&#39;level3&#39;&gt;
&lt;a href=&#39;#7_1_1&#39;&gt;7.1.1. 分音 (Partials)&lt;/a&gt;
&lt;a href=&#39;#7_1_2&#39;&gt;7.1.2. 嵌套 @import&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#7_2&#39;&gt;7.2. @media&lt;/a&gt;
&lt;a href=&#39;#7_3&#39;&gt;7.3. @extend&lt;/a&gt;
&lt;div class=&#39;level3&#39;&gt;
&lt;a href=&#39;#7_3_1&#39;&gt;7.3.1. How it Works&lt;/a&gt;
&lt;a href=&#39;#7_3_2&#39;&gt;7.3.2. 多重延伸 (Multiple Extends)&lt;/a&gt;
&lt;a href=&#39;#7_3_3&#39;&gt;7.3.3. 继续延伸 (Chaining Extends)&lt;/a&gt;
&lt;a href=&#39;#7_3_4&#39;&gt;7.3.4. 选择器列 (Selector Sequences)&lt;/a&gt;
&lt;a href=&#39;#7_3_5&#39;&gt;7.3.5. 合并选择器列 (Merging Selector Sequences)&lt;/a&gt;
&lt;a class=&#39;level4&#39; href=&#39;#7_3_5_1&#39;&gt;7.3.5.1. @extend-Only 选择器 (@extend-Only Selectors)&lt;/a&gt;
&lt;a href=&#39;#7_3_6&#39;&gt;7.3.6. @extend-Only 选择器 (@extend-Only Selectors)&lt;/a&gt;
&lt;a href=&#39;#7_3_7&#39;&gt;7.3.7. !optional 声明 (The !optional Flag)&lt;/a&gt;
&lt;a href=&#39;#7_3_8&#39;&gt;7.3.8. 在指令中延伸 (@extend in Directives)&lt;/a&gt;
&lt;/div&gt;
&lt;!--
&lt;a href=&quot;#7_4&quot;&gt;7.4. @debug&lt;/a&gt;
&lt;a href=&quot;#7_5&quot;&gt;7.5. @warn&lt;/a&gt;
--&gt;
&lt;/div&gt;
&lt;a href=&#39;#8&#39;&gt;8. 控制指令 (Control Directives)&lt;/a&gt;
&lt;div class=&#39;level2&#39;&gt;
&lt;a href=&#39;#8_1&#39;&gt;8.1. @if&lt;/a&gt;
&lt;a href=&#39;#8_2&#39;&gt;8.2. @for&lt;/a&gt;
&lt;a href=&#39;#8_3&#39;&gt;8.3. @each&lt;/a&gt;
&lt;a href=&#39;#8_4&#39;&gt;8.4. @while&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#9&#39;&gt;9. 混合指令 (Mixin Directives)&lt;/a&gt;
&lt;div class=&#39;level2&#39;&gt;
&lt;a href=&#39;#9_1&#39;&gt;9.1. 定义混合指令 @mixin (Defining a Mixin: @mixin)&lt;/a&gt;
&lt;a href=&#39;#9_2&#39;&gt;9.2. 引用混合样式 @include (Including a Mixin: @include)&lt;/a&gt;
&lt;a href=&#39;#9_3&#39;&gt;9.3. 参数 (Arguments)&lt;/a&gt;
&lt;div class=&#39;level3&#39;&gt;
&lt;a href=&#39;#9_3_1&#39;&gt;9.3.1. 关键词参数 (Keyword Arguments)&lt;/a&gt;
&lt;a href=&#39;#9_3_2&#39;&gt;9.3.2. 参数变量 (Variable Arguments)&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#9_4&#39;&gt;9.4. 向混合样式中导入内容 (Passing Content Blocks to a Mixin)&lt;/a&gt;
&lt;div class=&#39;level3&#39;&gt;
&lt;a href=&#39;#9_4_1&#39;&gt;9.4.1. Variable Scope and Content Blocks&lt;/a&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;a href=&#39;#10&#39;&gt;10. 函数指令 (Function Directives)&lt;/a&gt;
&lt;a href=&#39;#11&#39;&gt;11. 输出样式 (Output Style)&lt;/a&gt;
&lt;div class=&#39;level2&#39;&gt;
&lt;a href=&#39;#11_1&#39;&gt;11.1. :nested&lt;/a&gt;
&lt;a href=&#39;#11_2&#39;&gt;11.2. :expanded&lt;/a&gt;
&lt;a href=&#39;#11_3&#39;&gt;11.3. :compact&lt;/a&gt;
&lt;a href=&#39;#11_4&#39;&gt;11.4. :compressed&lt;/a&gt;
&lt;/div&gt;
&lt;a href=&#39;#12&#39;&gt;12. 拓展 Sass&lt;/a&gt;
&lt;div class=&#39;level2&#39;&gt;
&lt;a href=&#39;#12_1&#39;&gt;12.1. 自定义 Sass 函数&lt;/a&gt;
&lt;/div&gt;
&lt;/div&gt;&lt;h3 id=&#39;1&#39;&gt;1. 特色功能 (Features)&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;完全兼容 CSS3；&lt;/li&gt;
&lt;li&gt;在 CSS 语言基础上添加了拓展功能，比如变量、嵌套 (nesting)、混合 (mixin) 等等；&lt;/li&gt;
&lt;li&gt;可以用&lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Script/Functions.html&#39; target=&#39;_blank&#39;&gt;函数&lt;/a&gt;运算颜色值与属性值 (property value) ；&lt;/li&gt;
&lt;li&gt;高级命令，如&lt;a href=&#39;#8&#39;&gt;控制指令 (control directives)&lt;/a&gt;；&lt;/li&gt;
&lt;li&gt;可定义多种输出格式；&lt;/li&gt;
&lt;li&gt;&lt;a href=&#39;https://addons.mozilla.org/en-US/firefox/addon/firesass-for-firebug/&#39; target=&#39;_blank&#39;&gt;支持 Firebug&lt;/a&gt;。&lt;/li&gt;
&lt;/ul&gt;&lt;h3 id=&#39;2&#39;&gt;2. 语言格式 (Syntax)&lt;/h3&gt;
&lt;p&gt;Sass 包含两种语言格式。第一种被称为 SCSS (Sassy CSS)，也是本文示例所使用的格式，作为 CSS3 语言的拓展。也就是说，所有的 CSS3 样式都可以写进 SCSS 文件中，除此之外，SCSS 也能够理解大多数的 CSS hacks 以及浏览器前缀 (vendor-specific)，甚至早期的 IE 滤镜，这些都依赖于 Sass 的特色功能，下文将会详细介绍。这种格式以 &lt;code&gt;.scss&lt;/code&gt; 作为拓展名。&lt;/p&gt;

&lt;p&gt;另一种格式出现的时间更早一些，被成为缩进格式（或者 “Sass”），这是一种让书写 CSS 变得更简洁的方式。它使用“缩进”替代“括号”表示属性嵌套在选择器中，用“换行”替代“分号”表示属性的区分。不少人认为这种格式比 SCSS 更容易阅读，书写也更快速。缩进格式可使用 Sass 的所有功能，只是个别地方采用了不同的表达方式，具体可以通过 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/file.INDENTED_SYNTAX.html&#39; target=&#39;_blank&#39;&gt;the indented syntax reference&lt;/a&gt; 查看区别。这种格式以 &lt;code&gt;.sass&lt;/code&gt; 作为拓展名。&lt;/p&gt;

&lt;p&gt;每种格式都可以被&lt;a href=&#39;#7_1&#39;&gt;导入 (@import)&lt;/a&gt; 到另一种格式中使用，也可以通过 &lt;code&gt;sass-convert&lt;/code&gt; 指令互相转换。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;# Convert Sass to SCSS
$ sass-convert style.sass style.scss

# Convert SCSS to Sass
$ sass-convert style.scss style.sass&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h3 id=&#39;3&#39;&gt;3. 使用 Sass (Using Sass)&lt;/h3&gt;
&lt;p&gt;Sass 通过以下三种方式使用：在命令行工具中，作为独立的 Ruby 模式 (Ruby module)，或者作为 Rack-enabled 框架的插件（包括 Ruby on Rails 与 Merb）。第一步，安装 Sass gem （Windows 系统下，需要先&lt;a href=&#39;http://rubyinstaller.org/&#39; target=&#39;_blank&#39;&gt;安装 Ruby&lt;/a&gt;）：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;gem install sass&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;在命令行中运行 Sass：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;sass input.scss output.css&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;监视 Sass 文件，每次更新时，自动编译成 CSS：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;sass --watch input.scss:output.css&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;监视整个文件夹：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;sass --watch app/sass:public/stylesheets&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;更多命令请通过 &lt;code&gt;sass --help&lt;/code&gt; 查看。&lt;/p&gt;

&lt;p&gt;在 Ruby 代码中使用 Sass 也非常容易，安装完毕后，运行 &lt;code&gt;require &amp;quot;sass&amp;quot;&lt;/code&gt; 然后使用 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Engine.html&#39; target=&#39;_blank&#39;&gt;Sass::Engine&lt;/a&gt;，例如&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;engine = Sass::Engine.new(&amp;quot;#main {background-color: #0000ff}&amp;quot;, :syntax =&amp;gt; :scss)
engine.render #=&amp;gt; &amp;quot;#main { background-color: #0000ff; }\n&amp;quot;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;blockquote&gt;
&lt;p&gt;以下关于 Sass 设置的内容暂未翻译，请查看 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/file.SASS_REFERENCE.html#rackrailsmerb_plugin&#39; target=&#39;_blank&#39;&gt;原文档&lt;/a&gt;。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;!---
&lt;h4 id=&quot;3_1&quot;&gt;3.1. Rack/Rails/Merb Plugin&lt;/h4&gt;
&lt;h4 id=&quot;3_2&quot;&gt;3.2. Caching&lt;/h4&gt;
&lt;h4 id=&quot;3_3&quot;&gt;3.3. Options&lt;/h4&gt;
&lt;h4 id=&quot;3_4&quot;&gt;3.4. Syntax Selection&lt;/h4&gt;
&lt;h4 id=&quot;3_5&quot;&gt;3.5. Encodings&lt;/h4&gt;
&lt;h5 id=&quot;3_5_1&quot;&gt;3.5.1. Output Encoding&lt;/h5&gt;
--&gt;&lt;h3 id=&#39;4&#39;&gt;4. CSS 拓展 (CSS Extensions)&lt;/h3&gt;&lt;h4 id=&#39;4_1&#39;&gt;4.1. 嵌套规则 (Nested Rules)&lt;/h4&gt;
&lt;p&gt;Sass 可以将一套 CSS 样式嵌套进另一套样式中使用，嵌套后的样式将继承给它的外层选择器（父选择器），例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main p {
	color: #00ff00;
	width: 97%;

	.redbox {
		background-color: #ff0000;
		color: #000000;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main p {
	color: #00ff00;
	width: 97%; }
	#main p .redbox {
		background-color: #ff0000;
		color: #000000; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;嵌套功能可以避免重复使用父选择器，并且让书写复杂的 CSS 结构变得简单、容易，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	width: 97%;

	p, div {
		font-size: 2em;
		a { font-weight: bold; }
	}

	pre { font-size: 3em; }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	width: 97%; }
	#main p, #main div {
		font-size: 2em; }
		#main p a, #main div a {
			font-weight: bold; }
	#main pre {
		font-size: 3em; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;4_2&#39;&gt;4.2. 引用父选择器 &lt;code&gt;&amp;amp;&lt;/code&gt; (Referencing Parent Selectors: &lt;code&gt;&amp;amp;&lt;/code&gt;)&lt;/h4&gt;
&lt;p&gt;在嵌套 CSS 规则时，Sass 提供了一种简便的办法引用父选择器，比如，当给一个元素 hover 样式的时候，或者 &lt;code&gt;body&lt;/code&gt; 元素确定了 class 的时候。这时，可以用 &lt;code&gt;&amp;amp;&lt;/code&gt; 表示父选择器。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a {
	font-weight: bold;
	text-decoration: none;
	&amp;amp;:hover { text-decoration: underline; }
	body.firefox &amp;amp; { font-weight: normal; }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a {
	font-weight: bold;
	text-decoration: none; }
	a:hover {
		text-decoration: underline; }
	body.firefox a {
		font-weight: normal; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;在编译过的 CSS 文件中，&lt;code&gt;&amp;amp;&lt;/code&gt; 将被替换成外层的父选择器，如果使用了多层嵌套（最好不要超过三层），最外层的父选择器会被完整地替换给每一层。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	color: black;
	a {
		font-weight: bold;
		&amp;amp;:hover { color: red; }
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	color: black; }
	#main a {
		font-weight: bold; }
		#main a:hover {
			color: red; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;4_3&#39;&gt;4.3. 属性嵌套 (Nested Properties)&lt;/h4&gt;
&lt;p&gt;有些 CSS 属性遵循相同的命名规则 (namespace)，比如 &lt;code&gt;font-family, font-size, font-weight&lt;/code&gt; 都以 &lt;code&gt;font&lt;/code&gt; 作为属性名的前半部分（注，这个前半部分就是 namespace，也可单独作 CSS 属性）。使用一套遵循相同命名规则的属性，每次都要重复输入 namespace，略显繁琐。为此，Sass 提供了一种简便的方法，只需书写 namespace 一次，然后在其中嵌套子属性：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.funky {
	font: {
		family: fantasy;
		size: 30em;
		weight: bold;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.funky {
	font-family: fantasy;
	font-size: 30em;
	font-weight: bold; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;Namespace 也可以先设定属性值，再开始嵌套，比如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.funky {
	font: 2px/3px {
		family: fantasy;
		size: 30em;
		weight: bold;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.funky {
	font: 2px/3px;
		font-family: fantasy;
		font-size: 30em;
		font-weight: bold; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;4_4&#39;&gt;4.4. 占位符选择器 &lt;code&gt;%foo&lt;/code&gt; (Placeholder Selectors: &lt;code&gt;%foo&lt;/code&gt;)&lt;/h4&gt;
&lt;p&gt;Sass 支持一种特殊类型的选择器，占位符选择器 (placeholder selector)，看起来很像普通的 &lt;code&gt;id&lt;/code&gt; 或 &lt;code&gt;class&lt;/code&gt; 选择器，只是 &lt;code&gt;#&lt;/code&gt; 或 &lt;code&gt;.&lt;/code&gt; 被替换成了 &lt;code&gt;%&lt;/code&gt;。通过 &lt;a href=&#39;#7_3&#39;&gt;@extend&lt;/a&gt; 指令调用，更多信息请查看 &lt;a href=&#39;#7_3_6&#39;&gt;@extend-Only Selectors&lt;/a&gt;。&lt;/p&gt;

&lt;p&gt;当它们单独使用时（没有通过 &lt;code&gt;@extend&lt;/code&gt; 调用），不会被编译到 CSS 文件中。&lt;/p&gt;
&lt;h3 id=&#39;5&#39;&gt;5. 注释 &lt;code&gt;/* */&lt;/code&gt; 与 &lt;code&gt;//&lt;/code&gt;  (Comments: &lt;code&gt;/* */&lt;/code&gt; and &lt;code&gt;//&lt;/code&gt;)&lt;/h3&gt;
&lt;p&gt;Sass 使用 CSS 的多行注释 &lt;code&gt;/* */&lt;/code&gt;，同时也支持单行注释 &lt;code&gt;//&lt;/code&gt;，前者在编译时将被完整地输出到 CSS 文件中，后者则会被移除。例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;/* This comment is
 * several lines long.
 * since it uses the CSS comment syntax,
 * it will appear in the CSS output. */
body { color: black; }

// These comments are only one line long each.
// They won&amp;#39;t appear in the CSS output,
// since they use the single-line comment syntax.
a { color: green; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;/* This comment is
 * several lines long.
 * since it uses the CSS comment syntax,
 * it will appear in the CSS output. */
body {
	color: black; }

a {
	color: green; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;在压缩输出模式下 (compressed)，如果需要将注释编译到 CSS 文件中，可以将多行注释的第一个字母改为 &lt;code&gt;!&lt;/code&gt;，便于添加版权信息。&lt;/p&gt;
&lt;h3 id=&#39;6&#39;&gt;6. SassScript&lt;/h3&gt;
&lt;p&gt;除了与 CSS 一样的语法，Sass 也提供了一些被称为 SassScript 的拓展功能。 SassScript 可作用于任何属性，允许属性使用变量、算数运算等额外功能。在使用混合指令 (mixin) 时，SassScript 甚至可以生成选择器或属性名。&lt;/p&gt;
&lt;h4 id=&#39;6_1&#39;&gt;6.1. Interactive Shell&lt;/h4&gt;
&lt;p&gt;通过 Interactive Shell 测试 SassScript 的功能。在命令行中输入 &lt;code&gt;sass -i&lt;/code&gt;，然后，输入你想测试 SassScript 查看输出结果。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;$ sass -i
&amp;gt;&amp;gt; &amp;quot;Hello, Sassy World!&amp;quot;
&amp;quot;Hello, Sassy World!&amp;quot;
&amp;gt;&amp;gt; 1px + 1px + 1px
3px
&amp;gt;&amp;gt; #777 + #777
#eeeeee
&amp;gt;&amp;gt; #777 + #888
white&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;6_2&#39;&gt;6.2. 变量 &lt;code&gt;$&lt;/code&gt; (Variables: &lt;code&gt;$&lt;/code&gt;)&lt;/h4&gt;
&lt;p&gt;SassScript 最直接的用法就是变量，变量以美元符号开头，赋值方法与 CSS 属性的用法一样。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$width: 5em;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;然后可以这样调用变量&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	width: $width;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;嵌套规则内定义的变量只能在嵌套规则内使用，而嵌套规则外（最外层）定义的变量，则可以在任何地方使用。&lt;/p&gt;

&lt;p&gt;旧版本的 Sass 使用 &lt;code&gt;!&lt;/code&gt; 作为变量的前缀字符，用 &lt;code&gt;=&lt;/code&gt; 赋值，现在这两种方式仍然可以用，但不再鼓励使用，也会提示错误，所以请使用 &lt;code&gt;$&lt;/code&gt; 与 &lt;code&gt;:&lt;/code&gt;。&lt;/p&gt;
&lt;h4 id=&#39;6_3&#39;&gt;6.3. 数据类型 (Data Types)&lt;/h4&gt;
&lt;p&gt;SassScript 支持六种不同的数据类型：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;数字，&lt;code&gt;1, 2, 13, 10px&lt;/code&gt;；&lt;/li&gt;

&lt;li&gt;字符串，有引号字符串或无引号字符串，&lt;code&gt;&amp;quot;foo&amp;quot;, &amp;#39;bar&amp;#39;, baz&lt;/code&gt;；&lt;/li&gt;

&lt;li&gt;颜色，&lt;code&gt;blue, #04a3f9, rgba(255,0,0,0.5)&lt;/code&gt;；&lt;/li&gt;

&lt;li&gt;布尔型，&lt;code&gt;true, false&lt;/code&gt;；&lt;/li&gt;

&lt;li&gt;空值，&lt;code&gt;null&lt;/code&gt;；&lt;/li&gt;

&lt;li&gt;值列表，用空格或者逗号分开，&lt;code&gt;1.5em 1em 0 2em, Helvetica, Arial, sans-serif&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;SassScript 也支持其他 CSS 属性值（property value），比如 Unicode 范围，或 &lt;code&gt;!important&lt;/code&gt; 声明。然而，Sass 不会特殊对待这些属性值，一律视为无引号字符串 (unquoted strings)。&lt;/p&gt;
&lt;h5 id=&#39;6_3_1&#39;&gt;6.3.1. 字符串 (Strings)&lt;/h5&gt;
&lt;p&gt;SassScript 支持 CSS 的两种字符串类型：有引号字符串 (quoted strings)，如 &lt;code&gt;&amp;quot;Lucida Grande&amp;quot;&lt;/code&gt; &lt;code&gt;&amp;#39;http://sass-lang.com&amp;#39;&lt;/code&gt;；与无引号字符串 (unquoted strings)，如 &lt;code&gt;sans-serif&lt;/code&gt; &lt;code&gt;bold&lt;/code&gt;，在编译 CSS 文件时不会改变其类型。只有一种情况例外，使用 &lt;code&gt;#{ }&lt;/code&gt; 插值语句 (interpolation) 时，有引号字符串将被编译为无引号字符串，这样方便了在混合指令 (mixin) 中引用选择器名。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin firefox-message($selector) {
	body.firefox #{$selector}:before {
		content: &amp;quot;Hi, Firefox users!&amp;quot;;
	}
}

@include firefox-message(&amp;quot;.header&amp;quot;);&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;body.firefox .header:before {
	content: &amp;quot;Hi, Firefox users!&amp;quot;; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;需要注意的是：当 deprecated = property syntax 时 （暂时不理解是怎样的情况），所有的字符串都将被编译为无引号字符串，不论是否使用了引号。&lt;/p&gt;
&lt;h5 id=&#39;6_3_2&#39;&gt;6.3.2. 值列表 (Lists)&lt;/h5&gt;
&lt;p&gt;所谓值列表 (lists) 是指 Sass 如何处理 CSS 中 &lt;code&gt;margin: 10px 15px 0 0&lt;/code&gt; 或者 &lt;code&gt;font-face: Helvetica, Arial, sans-serif&lt;/code&gt; 这样通过空格或者逗号分隔的一系列的值。事实上，独立的值也被视为值列表——只包含一个值的值列表。&lt;/p&gt;
&lt;p&gt;&lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Script/Functions.html#list-functions&#39; target=&#39;_blank&#39;&gt;Sass list functions&lt;/a&gt; 赋予了值列表更多功能：nth function 可以直接访问值列表中的某一项，join function 可以将多个值列表连结在一起，append function 可以在值列表中添加值，而 @each rule 则能够给值列表中的每个项目添加样式。&lt;/p&gt;
&lt;p&gt;值列表中可以再包含值列表，比如 &lt;code&gt;1px 2px, 5px 6px&lt;/code&gt; 是包含 &lt;code&gt;1px 2px&lt;/code&gt; 与 &lt;code&gt;5px 6px&lt;/code&gt; 两个值列表的值列表。如果内外两层值列表使用相同的分隔方式，要用圆括号包裹内层，所以也可以写成 &lt;code&gt;(1px 2px) (5px 6px)&lt;/code&gt;。当值列表被编译为 CSS 时，Sass 不会添加任何圆括号，因为 CSS 不允许这样做。&lt;code&gt;(1px 2px) (5px 6px)&lt;/code&gt; 与 &lt;code&gt;1px 2px 5px 6px&lt;/code&gt; 在编译后的 CSS 文件中是一样的，但是它们在 Sass 文件中却有不同的意义，前者是包含两个值列表的值列表，而后者是包含四个值的值列表。&lt;/p&gt;

&lt;p&gt;可以用 &lt;code&gt;()&lt;/code&gt; 表示空的列表，这样不可以直接编译成 CSS，比如编译 &lt;code&gt;font-family: ()&lt;/code&gt; 时，Sass 将会报错。如果值列表中包含空的值列表或空值，编译时将清除空值，比如 &lt;code&gt;1px 2px () 3px&lt;/code&gt; 或 &lt;code&gt;1px 2px null 3px&lt;/code&gt;。&lt;/p&gt;
&lt;h4 id=&#39;6_4&#39;&gt;6.4. 运算符 (Operations)&lt;/h4&gt;
&lt;p&gt;所有数据类型均支持相等运算符 &lt;code&gt;==&lt;/code&gt; 或 &lt;code&gt;!=&lt;/code&gt;，此外，每种数据类型也有其特殊支持的运算符。&lt;/p&gt;
&lt;h5 id=&#39;6_4_1&#39;&gt;6.4.1. 数字运算 (Number Operations)&lt;/h5&gt;
&lt;p&gt;SassScript 支持数字的加减乘除、取整等运算 (&lt;code&gt;+, -, *, /, %&lt;/code&gt;)，如果需要也可以在不同单位间转换值。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	width: 1in + 8pt;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	width: 1.111in; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;关系运算符 &lt;code&gt;&amp;lt;, &amp;gt;, &amp;lt;=, &amp;gt;=&lt;/code&gt; 也可用于数字运算，相等运算符 &lt;code&gt;==, !=&lt;/code&gt; 可用于所有数据类型。&lt;/p&gt;
&lt;h5 id=&#39;6_4_1_1&#39;&gt;6.4.1.1. 除法运算 &lt;code&gt;/&lt;/code&gt; (Division and &lt;code&gt;/&lt;/code&gt;)&lt;/h5&gt;
&lt;p&gt;&lt;code&gt;/&lt;/code&gt; 在 CSS 中通常起到分隔数字的用途，SassScript 作为 CSS 语言的拓展当然也继承了这个功能，同时，赋予了 &lt;code&gt;/&lt;/code&gt; 除法运算的功能。也就是说，默认情况下，如果 &lt;code&gt;/&lt;/code&gt; 在 SassScript 中把两个数字分隔，编译的 CSS 文件中也是同样的作用。&lt;/p&gt;

&lt;p&gt;以下三种情况下 &lt;code&gt;/&lt;/code&gt; 将被视为除法运算符号（通常只有这三种情况）：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果值，或值的一部分，是变量；&lt;/li&gt;
&lt;li&gt;如果值被圆括号包裹；&lt;/li&gt;
&lt;li&gt;如果值是算数表达式的一部分。&lt;/li&gt;
&lt;/ul&gt;&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	font: 10px/8px;				  // Plain CSS, no division
	$width: 1000px;
	width: $width/2;			  // Uses a variable, does division
	height: (500px/2);            // Uses parentheses, does division
	margin-left: 5px + 8px/2px;   // Uses +, does division
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	font: 10px/8px;
	width: 500px;
	height: 250px;
	margin-left: 9px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;如果需要使用变量，同时又要确保 &lt;code&gt;/&lt;/code&gt; 不做除法运算而是完整地编译到 CSS 文件中起分隔的作用，只需要用 &lt;code&gt;#{ }&lt;/code&gt; 插值语句将变量包裹。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	$font-size: 12px;
	$line-height: 30px;
	font: #{$font-size}/#{$line-height};
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	font: 12px/30px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;6_4_2&#39;&gt;6.4.2. 颜色值运算 (Color Operations)&lt;/h5&gt;
&lt;p&gt;颜色值的运算是分段计算进行的，也就是分别计算红色，绿色，以及蓝色的值：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: #010203 + #040506;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;计算 &lt;code&gt;01 + 04 = 05&lt;/code&gt; &lt;code&gt;02 + 05 = 07&lt;/code&gt; &lt;code&gt;03 + 06 = 09&lt;/code&gt;，然后编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: #050709; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;通常，使用 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Script/Functions.html&#39; target=&#39;_blank&#39;&gt;color functions&lt;/a&gt; 比计算颜色值更方便一些。&lt;/p&gt;

&lt;p&gt;数字与颜色值之间也可以进行算数运算，同样也是分段计算的，比如&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: #010203 * 2;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;计算 &lt;code&gt;01 * 2 = 02&lt;/code&gt; &lt;code&gt;02 * 2 = 04&lt;/code&gt; &lt;code&gt;03 * 2 = 06&lt;/code&gt;，然后编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: #020406; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;需要注意的是，如果颜色值包含 alpha channel（rgba 或 hsla 两种颜色值），必须拥有相等的 alpha 值才能进行运算，因为算术运算不会作用于 alpha 值。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: rgba(255, 0, 0, 0.75) + rgba(0, 255, 0, 0.75);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: rgba(255, 255, 0, 0.75); }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;颜色值的 alpha channel 可以通过 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Script/Functions.html#opacify-instance_method&#39; target=&#39;_blank&#39;&gt;opacify&lt;/a&gt; 或 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Script/Functions.html#transparentize-instance_method&#39; target=&#39;_blank&#39;&gt;transparentize&lt;/a&gt; 两个函数进行调整。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$translucent-red: rgba(255, 0, 0, 0.5);
p {
	color: opacify($translucent-red, 0.3);
	background-color: transparentize($translucent-red, 0.25);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: rgba(255, 0, 0, 0.8);
	background-color: rgba(255, 0, 0, 0.25); }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;IE 滤镜要求所有的颜色值包含 alpha 层，而且格式必须固定 &lt;code&gt;#AABBCCDD&lt;/code&gt;，使用 &lt;code&gt;ie_hex_str&lt;/code&gt; 函数可以很容易地将颜色转化为 IE 滤镜要求的格式。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$translucent-red: rgba(255, 0, 0, 0.5);
$green: #00ff00;
div {
	filter: progid:DXImageTransform.Microsoft.gradient(enabled=&amp;#39;false&amp;#39;, startColorstr=&amp;#39;#{ie-hex-str($green)}&amp;#39;, endColorstr=&amp;#39;#{ie-hex-str($translucent-red)}&amp;#39;);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;div {
	filter: progid:DXImageTransform.Microsoft.gradient(enabled=&amp;#39;false&amp;#39;, startColorstr=#FF00FF00, endColorstr=#80FF0000);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;6_4_3&#39;&gt;6.4.3. 字符串运算 (String Operations)&lt;/h5&gt;
&lt;p&gt;&lt;code&gt;+&lt;/code&gt; 可用于连结字符串&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	cursor: e + -resize;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	cursor: e-resize; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;注意，如果有引号字符串（&lt;code&gt;+&lt;/code&gt; 左侧）连结无引号字符串，运算结果是有引号的，反过来，无引号字符串（&lt;code&gt;+&lt;/code&gt; 左侧）连结有引号字符串，运算结果则没有引号。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p:before {
	content: &amp;quot;Foo &amp;quot; + Bar;
	font-family: sans- + &amp;quot;serif&amp;quot;;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p:before {
	content: &amp;quot;Foo Bar&amp;quot;;
	font-family: sans-serif; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;运算表达式与其他值连用时，用空格做连接符：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	margin: 3px + 4px auto;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	margin: 7px auto; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;在文本字符串中 &lt;code&gt;#{ }&lt;/code&gt; 插值语句可以被用来添加动态的值：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p:before {
	content: &amp;quot;I ate #{5 + 10} pies!&amp;quot;;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p:before {
	content: &amp;quot;I ate 15 pies!&amp;quot;; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;空的值被视作插入了空字符串：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$value: null;
p:before {
	content: &amp;quot;I ate #{$value} pies!&amp;quot;;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p:before {
	content: &amp;quot;I ate	pies!&amp;quot;; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;6_4_4&#39;&gt;6.4.4. 布尔运算 (Boolean Operations)&lt;/h5&gt;
&lt;p&gt;SassScript 支持布尔型的 &lt;code&gt;and&lt;/code&gt; &lt;code&gt;or&lt;/code&gt; 以及 &lt;code&gt;not&lt;/code&gt; 运算。&lt;/p&gt;
&lt;h5 id=&#39;6_4_5&#39;&gt;6.4.5. 值列表运算 (List Operations)&lt;/h5&gt;
&lt;p&gt;值列表不支持任何运算方式，只能使用 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Script/Functions.html#list-functions&#39; target=&#39;_blank&#39;&gt;list functions&lt;/a&gt; 控制。&lt;/p&gt;
&lt;h4 id=&#39;6_5&#39;&gt;6.5. 圆括号 (Parentheses)&lt;/h4&gt;
&lt;p&gt;圆括号可以用来影响运算的顺序：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	width: 1em + (2em * 3);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	width: 7em; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;6_6&#39;&gt;6.6. 函数 (Functions)&lt;/h4&gt;
&lt;p&gt;SassScript 定义了多种函数，有些甚至可以通过普通的 CSS 语句调用：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: hsl(0, 100%, 50%);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: #ff0000; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;6_6_1&#39;&gt;6.6.1. 关键词参数 (Keyword Arguments)&lt;/h5&gt;
&lt;p&gt;Sass 函数允许使用关键词参数 (keyword arguments)，上面的例子也可以写成：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: hsl($hue: 0, $saturation: 100%, $lightness: 50%);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;虽然不够简明，但是阅读起来会更方便。关键词参数给函数提供了更灵活的接口，以及容易调用的参数。关键词参数可以打乱顺序使用，如果使用默认值也可以省缺，另外，参数名被视为变量名，下划线、短横线可以互换使用。&lt;/p&gt;

&lt;p&gt;通过 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Script/Functions.html&#39; target=&#39;_blank&#39;&gt;Sass::Script::Functions&lt;/a&gt; 查看完整的 Sass 函数列表，参数名，以及如何自定义函数。&lt;/p&gt;
&lt;h4 id=&#39;6_7&#39;&gt;6.7. 插值语句 &lt;code&gt;#{ }&lt;/code&gt; (Interpolation: &lt;code&gt;#{ }&lt;/code&gt;)&lt;/h4&gt;
&lt;p&gt;通过 &lt;code&gt;#{ }&lt;/code&gt; 插值语句可以在选择器或属性名中使用变量：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$name: foo;
$attr: border;
p.#{$name} {
	#{$attr}-color: blue;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p.foo {
	border-color: blue; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;#{ }&lt;/code&gt; 插值语句也可以在属性值中插入 SassScript，大多数情况下，这样可能还不如使用变量方便，但是使用 &lt;code&gt;#{ }&lt;/code&gt; 可以避免 Sass 运行运算表达式，直接编译 CSS。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	$font-size: 12px;
	$line-height: 30px;
	font: #{$font-size}/#{$line-height};
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	font: 12px/30px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;6_8&#39;&gt;6.8. 变量定义 &lt;code&gt;!default&lt;/code&gt; (Variable Defaults: &lt;code&gt;!default&lt;/code&gt;)&lt;/h4&gt;
&lt;p&gt;可以在变量的结尾添加 &lt;code&gt;!default&lt;/code&gt; 给一个未通过 &lt;code&gt;!default&lt;/code&gt; 声明赋值的变量赋值，此时，如果变量已经被赋值，不会再被重新赋值，但是如果变量还没有被赋值，则会被赋予新的值。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$content: &amp;quot;First content&amp;quot;;
$content: &amp;quot;Second content?&amp;quot; !default;
$new_content: &amp;quot;First time reference&amp;quot; !default;

#main {
	content: $content;
	new-content: $new_content;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	content: &amp;quot;First content&amp;quot;;
	new-content: &amp;quot;First time reference&amp;quot;; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;变量是 null 空值时将视为未被 &lt;code&gt;!default&lt;/code&gt; 赋值。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$content: null;
$content: &amp;quot;Non-null content&amp;quot; !default;

#main {
	content: $content;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	content: &amp;quot;Non-null content&amp;quot;; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;h3 id=&#39;7&#39;&gt;7. @-Rules 与指令 (@-Rules and Directives)&lt;/h3&gt;&lt;/p&gt;

&lt;p&gt;Sass 支持所有的 CSS3 @-Rules，以及 Sass 特有的 “指令”（directives）。这一节会详细解释，更多资料请查看 &lt;a href=&#39;#8&#39;&gt;控制指令 (control directives)&lt;/a&gt; 与 &lt;a href=&#39;#9&#39;&gt;混合指令 (mixin directives)&lt;/a&gt; 两个部分。&lt;/p&gt;

&lt;p&gt;&lt;h4 id=&#39;7_1&#39;&gt;7.1. @import&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;Sass 拓展了 &lt;code&gt;@import&lt;/code&gt; 的功能，允许其导入 SCSS 或 Sass 文件。被导入的文件将合并编译到同一个 CSS 文件中，另外，被导入的文件中所包含的变量或者混合指令 (mixin) 都可以在导入的文件中使用。&lt;/p&gt;

&lt;p&gt;Sass 在当前地址，或 Rack, Rails, Merb 的 Sass 文件地址寻找 Sass 文件，如果需要设定其他地址，可以用 &lt;code&gt;:load_paths&lt;/code&gt; 选项，或者在命令行中输入 &lt;code&gt;--load-path&lt;/code&gt; 命令。&lt;/p&gt;

&lt;p&gt;通常，&lt;code&gt;@import&lt;/code&gt; 寻找 Sass 文件并将其导入，但在以下情况下，&lt;code&gt;@import&lt;/code&gt; 仅作为普通的 CSS 语句，不会导入任何 Sass 文件。&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;文件拓展名是 &lt;code&gt;.css&lt;/code&gt;；&lt;/li&gt;

&lt;li&gt;文件名以 &lt;code&gt;http://&lt;/code&gt; 开头；&lt;/li&gt;

&lt;li&gt;文件名是 &lt;code&gt;url()&lt;/code&gt;；&lt;/li&gt;

&lt;li&gt;&lt;code&gt;@import&lt;/code&gt; 包含 media queries。&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;如果不在上述情况内，文件的拓展名是 &lt;code&gt;.scss&lt;/code&gt; 或 &lt;code&gt;.sass&lt;/code&gt;，则导入成功。没有指定拓展名，Sass 将会试着寻找文件名相同，拓展名为 &lt;code&gt;.scss&lt;/code&gt; 或 &lt;code&gt;.sass&lt;/code&gt; 的文件并将其导入。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@import &amp;quot;foo.scss&amp;quot;;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;或&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@import &amp;quot;foo&amp;quot;;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;都会导入文件 foo.scss，但是&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@import &amp;quot;foo.css&amp;quot;;
@import &amp;quot;foo&amp;quot; screen;
@import &amp;quot;http://foo.com/bar&amp;quot;;
@import url(foo);&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@import &amp;quot;foo.css&amp;quot;;
@import &amp;quot;foo&amp;quot; screen;
@import &amp;quot;http://foo.com/bar&amp;quot;;
@import url(foo);&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;Sass 允许同时导入多个文件，例如同时导入 rounded-corners 与 text-shadow 两个文件：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@import &amp;quot;rounded-corners&amp;quot;, &amp;quot;text-shadow&amp;quot;;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;导入文件也可以使用 &lt;code&gt;#{ }&lt;/code&gt; 插值语句，但不是通过变量动态导入 Sass 文件，只能作用于 CSS 的 &lt;code&gt;url()&lt;/code&gt; 导入方式：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$family: unquote(&amp;quot;Droid+Sans&amp;quot;);
@import url(&amp;quot;http://fonts.googleapis.com/css?family=\#{$family}&amp;quot;);&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@import url(&amp;quot;http://fonts.googleapis.com/css?family=Droid+Sans&amp;quot;);&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;7_1_1&#39;&gt;7.1.1. 分音 (Partials)&lt;/h5&gt;
&lt;p&gt;如果需要导入 SCSS 或者 Sass 文件，但又不希望将其编译为 CSS，只需要在文件名前添加下划线，这样会告诉 Sass 不要编译这些文件，但导入语句中却不需要添加下划线。&lt;/p&gt;

&lt;p&gt;例如，将文件命名为 &lt;code&gt;_colors.scss&lt;/code&gt;，便不会编译 &lt;code&gt;_colours.css&lt;/code&gt; 文件。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@import &amp;quot;colors&amp;quot;;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;上面的例子，导入的其实是 &lt;code&gt;_colors.scss&lt;/code&gt; 文件&lt;/p&gt;

&lt;p&gt;注意，不可以同时存在添加下划线与未添加下划线的同名文件，添加下划线的文件将会被忽略。&lt;/p&gt;

&lt;p&gt;&lt;h5 id=&#39;7_1_2&#39;&gt;7.1.2. 嵌套 @import&lt;/h5&gt;&lt;/p&gt;

&lt;p&gt;大多数情况下，一般在文件的最外层（不在嵌套规则内）使用 &lt;code&gt;@import&lt;/code&gt;，其实，也可以将 &lt;code&gt;@import&lt;/code&gt; 嵌套进 CSS 样式或者 &lt;code&gt;@media&lt;/code&gt; 中，与平时的用法效果相同，只是这样导入的样式只能出现在嵌套的层中。&lt;/p&gt;

&lt;p&gt;假设 example.scss 文件包含以下样式：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.example {
	color: red;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;然后导入到 &lt;code&gt;#main&lt;/code&gt; 样式内&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	@import &amp;quot;example&amp;quot;;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;将会被编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main .example {
	color: red;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;blockquote&gt;
&lt;p&gt;Directives that are only allowed at the base level of a document, like @mixin or @charset, are not allowed in files that are @imported in a nested context. 这一句不理解&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;不可以在混合指令 (mixin) 或控制指令 (control directives) 中嵌套 &lt;code&gt;@import&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;&lt;h4 id=&#39;7_2&#39;&gt;7.2. @media&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;Sass 中 &lt;code&gt;@media&lt;/code&gt; 指令与 CSS 中用法一样，只是增加了一点额外的功能：允许其在 CSS 规则中嵌套。如果 &lt;code&gt;@media&lt;/code&gt; 嵌套在 CSS 规则内，编译时，&lt;code&gt;@media&lt;/code&gt; 将被编译到文件的最外层，包含嵌套的父选择器。这个功能让 &lt;code&gt;@media&lt;/code&gt; 用起来更方便，不需要重复使用选择器，也不会打乱 CSS 的书写流程。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.sidebar {
	width: 300px;
	@media screen and (orientation: landscape) {
		width: 500px;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.sidebar {
	width: 300px; }
	@media screen and (orientation: landscape) {
		.sidebar {
			width: 500px; } }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;@media&lt;/code&gt; 的 queries 允许互相嵌套使用，编译时，Sass 自动添加 &lt;code&gt;and&lt;/code&gt;&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@media screen {
	.sidebar {
		@media (orientation: landscape) {
			width: 500px;
		}
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@media screen and (orientation: landscape) {
	.sidebar {
		width: 500px; } }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;@media&lt;/code&gt; 甚至可以使用 SassScript（比如变量，函数，以及运算符）代替条件的名称或者值：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$media: screen;
$feature: -webkit-min-device-pixel-ratio;
$value: 1.5;

@media #{$media} and ($feature: $value) {
	.sidebar {
		width: 500px;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@media screen and (-webkit-min-device-pixel-ratio: 1.5) {
	.sidebar {
		width: 500px; } }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;h4 id=&#39;7_3&#39;&gt;7.3. @extend&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;在设计网页的时候常常遇到这种情况：一个元素使用的样式与另一个元素完全相同，但又添加了额外的样式。通常会在 HTML 中给元素定义两个 class，一个通用样式，一个特殊样式。假设现在要设计一个普通错误样式与一个严重错误样式，一般会这样写：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;&amp;lt;div class=&amp;quot;error seriousError&amp;quot;&amp;gt;
	Oh no! You&amp;#39;ve been hacked!
&amp;lt;/div&amp;gt;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;样式如下&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error {
	border: 1px #f00;
	background-color: #fdd;
}
.seriousError {
	border-width: 3px;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;麻烦的是，这样做必须时刻记住使用 &lt;code&gt;.seriousError&lt;/code&gt; 时需要参考 &lt;code&gt;.error&lt;/code&gt; 的样式，带来了很多不变：智能比如加重维护负担，导致 bug，或者给 HTML 添加无语意的样式。使用 &lt;code&gt;@extend&lt;/code&gt; 可以避免上述情况，告诉 Sass 将一个选择器下的所有样式继承给另一个选择器。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error {
	border: 1px #f00;
	background-color: #fdd;
}
.seriousError {
	@extend .error;
	border-width: 3px;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;上面代码的意思是将 &lt;code&gt;.error&lt;/code&gt; 下的所有样式继承给 &lt;code&gt;.seriousError&lt;/code&gt;，&lt;code&gt;border-width: 3px;&lt;/code&gt; 是单独给 &lt;code&gt;.seriousError&lt;/code&gt; 设定特殊样式，这样，使用 &lt;code&gt;.seriousError&lt;/code&gt; 的地方可以不再使用 &lt;code&gt;.error&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;其他使用到 &lt;code&gt;.error&lt;/code&gt; 的样式也会同样继承给 &lt;code&gt;.seriousError&lt;/code&gt;，例如，另一个样式 &lt;code&gt;.error.intrusion&lt;/code&gt; 使用了 &lt;code&gt;hacked.png&lt;/code&gt; 做背景，&lt;code&gt;&amp;lt;div class=&amp;quot;seriousError intrusion&amp;quot;&amp;gt;&lt;/code&gt; 也同样会使用 &lt;code&gt;hacked.png&lt;/code&gt; 背景。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error.intrusion {
	background-image: url(&amp;quot;/image/hacked.png&amp;quot;);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;7_3_1&#39;&gt;7.3.1. How it Works&lt;/h5&gt;
&lt;p&gt;&lt;code&gt;@extend&lt;/code&gt; 的作用是将重复使用的样式 (&lt;code&gt;.error&lt;/code&gt;) 延伸 (extend) 给需要包含这个样式的特殊样式（&lt;code&gt;.seriousError&lt;/code&gt;），刚刚的例子：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error {
	border: 1px #f00;
	background-color: #fdd;
}
.error.intrusion {
	background-image: url(&amp;quot;/image/hacked.png&amp;quot;);
}
.seriousError {
	@extend .error;
	border-width: 3px;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error, .seriousError {
	border: 1px #f00;
	background-color: #fdd; }

.error.intrusion, .seriousError.intrusion {
	background-image: url(&amp;quot;/image/hacked.png&amp;quot;); }

.seriousError {
	border-width: 3px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;当合并选择器时，&lt;code&gt;@extend&lt;/code&gt; 会很聪明地避免无谓的重复，&lt;code&gt;.seriousError.seriousError&lt;/code&gt; 将编译为 &lt;code&gt;.seriousError&lt;/code&gt;，不能匹配任何元素的选择器（比如 &lt;code&gt;#main#footer&lt;/code&gt; ）也会删除。&lt;/p&gt;
&lt;h5 id=&#39;7_3_2&#39;&gt;7.3.2. 延伸复杂的选择器 (Extending Complex Selectors)&lt;/h5&gt;
&lt;p&gt;Class 选择器并不是唯一可以被延伸 (extend) 的，Sass 允许延伸任何定义给单个元素的选择器，比如 &lt;code&gt;.special.cool&lt;/code&gt;，&lt;code&gt;a:hover&lt;/code&gt; 或者 &lt;code&gt;a.user[href^=&amp;quot;http://&amp;quot;]&lt;/code&gt; 等，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.hoverlink {
	@extend a:hover;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;同 class 元素一样，&lt;code&gt;a:hover&lt;/code&gt; 的样式将继承给 &lt;code&gt;.hoverlink&lt;/code&gt;。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.hoverlink {
	@extend a:hover;
}
a:hover {
	text-decoration: underline;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a:hover, .hoverlink {
	text-decoration: underline; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;与上面 &lt;code&gt;.error.intrusion&lt;/code&gt; 的例子一样，所有 &lt;code&gt;a:hover&lt;/code&gt; 的样式将继承给 &lt;code&gt;.hoverlink&lt;/code&gt;，包括其他使用到 &lt;code&gt;a:hover&lt;/code&gt; 的样式，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.hoverlink {
	@extend a:hover;
}
.comment a.user:hover {
	font-weight: bold;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.comment a.user:hover, .comment .user.hoverlink {
	font-weight: bold; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;7_3_3&#39;&gt;7.3.3. 多重延伸 (Multiple Extends)&lt;/h5&gt;
&lt;p&gt;同一个选择器可以延伸给多个选择器，它所包含的属性将继承给所有被延伸的选择器：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error {
	border: 1px #f00;
	background-color: #fdd;
}
.attention {
	font-size: 3em;
	background-color: #ff0;
}
.seriousError {
	@extend .error;
	@extend .attention;
	border-width: 3px;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error, .seriousError {
	border: 1px #f00;
	background-color: #fdd; }

.attention, .seriousError {
	font-size: 3em;
	background-color: #ff0; }

.seriousError {
	border-width: 3px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;每个 &lt;code&gt;.seriousError&lt;/code&gt; 将包含 &lt;code&gt;.error&lt;/code&gt; 与 &lt;code&gt;.attention&lt;/code&gt; 下的所有样式，这时，后定义的样式享有优先权：&lt;code&gt;.seriousError&lt;/code&gt; 的背景颜色是 &lt;code&gt;#ff0&lt;/code&gt; 而不是 &lt;code&gt;#fdd&lt;/code&gt;，因为 &lt;code&gt;.attention&lt;/code&gt; 在 &lt;code&gt;.error&lt;/code&gt; 之后定义。&lt;/p&gt;

&lt;p&gt;多重延伸可以使用逗号分隔选择器名，比如 &lt;code&gt;@extend .error, .attention;&lt;/code&gt; 与 &lt;code&gt;@extend .error;&lt;/code&gt; &lt;code&gt;@extend.attention&lt;/code&gt; 有相同的效果。&lt;/p&gt;
&lt;h5 id=&#39;7_3_4&#39;&gt;7.3.4. 继续延伸 (Chaining Extends)&lt;/h5&gt;
&lt;p&gt;当一个选择器延伸给第二个后，可以继续将第二个选择器延伸给第三个，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error {
	border: 1px #f00;
	background-color: #fdd;
}
.seriousError {
	@extend .error;
	border-width: 3px;
}
.criticalError {
	@extend .seriousError;
	position: fixed;
	top: 10%;
	bottom: 10%;
	left: 10%;
	right: 10%;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;现在，每个 &lt;code&gt;.seriousError&lt;/code&gt; 选择器将包含 &lt;code&gt;.error&lt;/code&gt; 的样式，而 &lt;code&gt;.criticalError&lt;/code&gt; 不仅包含 &lt;code&gt;.seriousError&lt;/code&gt; 的样式也会同时包含 &lt;code&gt;.error&lt;/code&gt; 的所有样式，上面的代码编译为：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error, .seriousError, .criticalError {
	border: 1px #f00;
	background-color: #fdd; }

.seriousError, .criticalError {
	border-width: 3px; }

.criticalError {
	position: fixed;
	top: 10%;
	bottom: 10%;
	left: 10%;
	right: 10%; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;7_3_5&#39;&gt;7.3.5. 选择器列 (Selector Sequences)&lt;/h5&gt;
&lt;p&gt;暂时不可以将选择器列 (Selector Sequences)，比如 &lt;code&gt;.foo .bar&lt;/code&gt; 或 &lt;code&gt;.foo + .bar&lt;/code&gt;，延伸给其他元素，但是，却可以将其他元素延伸给选择器列：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#fake-links .link {
	@extend a;
}

a {
	color: blue;
	&amp;amp;:hover {
		text-decoration: underline;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a, #fake-links .link {
	color: blue; }
	a:hover, #fake-links .link:hover {
		text-decoration: underline; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;7_3_5_1&#39;&gt;7.3.5.1 合并选择器列 (Merging Selector Sequences)&lt;/h5&gt;
&lt;p&gt;有时会遇到复杂的情况，比如选择器列中的某个元素需要延伸给另一个选择器列，这种情况下，两个选择器列需要合并，比如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#admin .tabbar a {
	font-weight: bold;
}
#demo .overview .fakelink {
	@extend a;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;技术上讲能够生成所有匹配条件的结果，但是这样生成的样式表太复杂了，上面这个简单的例子就可能有 10 种结果。所以，Sass 只会编译输出有用的选择器。&lt;/p&gt;

&lt;p&gt;当两个列 (sequence) 合并时，如果没有包含相同的选择器，将生成两个新选择器：第一列出现在第二列之前，或者第二列出现在第一列之前：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#admin .tabbar a {
	font-weight: bold;
}
#demo .overview .fakelink {
	@extend a;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#admin .tabbar a,
#admin .tabbar #demo .overview .fakelink,
#demo .overview #admin .tabbar .fakelink {
	font-weight: bold; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;如果两个列 (sequence) 包含了相同的选择器，相同部分将会合并在一起，其他部分交替输出。在下面的例子里，两个列都包含 &lt;code&gt;#admin&lt;/code&gt;，输出结果中它们合并在了一起：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#admin .tabbar a {
	font-weight: bold;
}
#admin .overview .fakelink {
	@extend a;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#admin .tabbar a,
#admin .tabbar .overview .fakelink,
#admin .overview .tabbar .fakelink {
	font-weight: bold; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;h5 id=&#39;7_3_6&#39;&gt;7.3.6. @extend-Only 选择器 (@extend-Only Selectors)&lt;/h5&gt;&lt;/p&gt;

&lt;p&gt;有时，需要定义一套样式并不是给某个元素用，而是只通过 &lt;code&gt;@extend&lt;/code&gt; 指令使用，尤其是在制作 Sass 样式库的时候，希望 Sass 能够忽略用不到的样式。&lt;/p&gt;

&lt;p&gt;如果使用普通的 CSS 规则，最后会编译出很多用不到的样式，也容易与其他样式名冲突，所以，Sass 引入了“占位符选择器” (placeholder selectors)，看起来很像普通的 &lt;code&gt;id&lt;/code&gt; 或 &lt;code&gt;class&lt;/code&gt; 选择器，只是 &lt;code&gt;#&lt;/code&gt; 或 &lt;code&gt;.&lt;/code&gt; 被替换成了 &lt;code&gt;%&lt;/code&gt;。可以像 class 或者 id 选择器那样使用，当它们单独使用时，不会被编译到 CSS 文件中。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;// This ruleset won&amp;#39;t be rendered on its own.
#context a%extreme {
	color: blue;
	font-weight: bold;
	font-size: 2em;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;占位符选择器需要通过延伸指令使用，用法与 class 或者 id 选择器一样，被延伸后，占位符选择器本身不会被编译。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.notice {
	@extend %extreme;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#context a.notice {
	color: blue;
	font-weight: bold;
	font-size: 2em; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;7_3_7&#39;&gt;7.3.7. &lt;code&gt;!optional&lt;/code&gt; 声明 (The &lt;code&gt;!optional&lt;/code&gt; Flag)&lt;/h5&gt;
&lt;p&gt;如果 &lt;code&gt;@extend&lt;/code&gt; 失败会收到错误提示，比如，这样写 &lt;code&gt;a.important {@extend .notice}&lt;/code&gt;，当没有 &lt;code&gt;.notice&lt;/code&gt; 选择器时，将会报错，只有 &lt;code&gt;h1.notice&lt;/code&gt; 包含 &lt;code&gt;.notice&lt;/code&gt; 时也会报错，因为 &lt;code&gt;h1&lt;/code&gt; 与 &lt;code&gt;a&lt;/code&gt; 冲突，会生成新的选择器。&lt;/p&gt;

&lt;p&gt;如果要求 &lt;code&gt;@extend&lt;/code&gt; 不生成新选择器，可以通过 &lt;code&gt;!optional&lt;/code&gt; 声明达到这个目的，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a.important {
	@extend .notice !optional;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;h5 id=&#39;7_3_8&#39;&gt;7.3.8 在指令中延伸 (@extend in Directives)&lt;/h5&gt;&lt;/p&gt;

&lt;p&gt;在指令中使用 &lt;code&gt;@extend&lt;/code&gt; 时（比如在 &lt;code&gt;@media&lt;/code&gt; 中）有一些限制：Sass 不可以将 &lt;code&gt;@media&lt;/code&gt; 层外的 CSS 规则延伸给指令层内的 CSS，这样会生成大量的无用代码。也就是说，如果在 &lt;code&gt;@media&lt;/code&gt; （或者其他 CSS 指令）中使用 &lt;code&gt;@extend&lt;/code&gt;，必须延伸给相同指令层中的选择器。&lt;/p&gt;

&lt;p&gt;下面的例子是可行的：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@media print {
	.error {
		border: 1px #f00;
		background-color: #fdd;
	}
	.seriousError {
		@extend .error;
		border-width: 3px;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;但不可以这样：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.error {
	border: 1px #f00;
	background-color: #fdd;
}

@media print {
	.seriousError {
		// INVALID EXTEND: .error is used outside of the &amp;quot;@media print&amp;quot; directive
		@extend .error;
		border-width: 3px;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;希望有一天，浏览器可以原生支持 &lt;code&gt;@extend&lt;/code&gt; 指令，这样就可以在任何指令中使用延伸功能，不再受限制了。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;&lt;code&gt;@debug&lt;/code&gt; 与 &lt;code&gt;@warn&lt;/code&gt; 两条指令不太熟悉，暂时未翻译，请查看 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/file.SASS_REFERENCE.html#id8&#39; target=&#39;_blank&#39;&gt;原文档&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;!--
&lt;h4 id=&quot;7_4&quot;&gt;7.4. @debug&lt;/h4&gt;

&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@debug 10em + 12em;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;


&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;Line 1 DEBUG: 22em&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;


&lt;h4 id=&quot;7_5&quot;&gt;7.5 @warn&lt;/h4&gt;

&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin adjust-location($x, $y) {
	@if unitless($x) {
		@warn &amp;quot;Assuming #{$x} to be in pixels&amp;quot;;
		$x: 1px * $x;
	}
	@if unitless($y) {
		@warn &amp;quot;Assuming #{$y} to be in pixels&amp;quot;;
		$y: 1px * $y;
	}
	position: relative; left: $x; top: $y;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;

--&gt;&lt;h3 id=&#39;8&#39;&gt;8. 控制指令 (Control Directives)&lt;/h3&gt;
&lt;p&gt;SassScript 提供了一些基础的控制指令，比如在满足条件的时候引用样式，或者在一定范围内重复输出样式。&lt;strong&gt;请注意，控制指令是一种高级功能，不建议频繁使用。&lt;/strong&gt;控制指令主要与混合指令 (mixin) 配合使用，尤其是在 &lt;a href=&#39;http://compass-style.org/&#39; target=&#39;_blank&#39;&gt;Compass&lt;/a&gt; 等样式库中。&lt;/p&gt;

&lt;p&gt;&lt;h4 id=&#39;8_1&#39;&gt;8.1. @if&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;@if&lt;/code&gt; 指令内包含多个条件语句，当返回值不是 &lt;code&gt;false&lt;/code&gt; 或者 &lt;code&gt;null&lt;/code&gt; 时，条件成立，输出结果：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	@if 1 + 1 == 2 { border: 1px solid;	}
	@if 5 &amp;lt; 3	   { border: 2px dotted; }
	@if null	   { border: 3px double; }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	border: 1px solid; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;code&gt;@if&lt;/code&gt; 声明后面可跟多个 &lt;code&gt;@else if&lt;/code&gt; 声明，或者一个 &lt;code&gt;@else&lt;/code&gt; 声明。如果 &lt;code&gt;@if&lt;/code&gt; 声明失败，Sass 将逐条执行 &lt;code&gt;@else if&lt;/code&gt; 声明，如果仍然失败，执行 &lt;code&gt;@else&lt;/code&gt; 声明，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$type: monster;
p {
	@if $type == ocean {
		color: blue;
	} @else if $type == matador {
		color: red;
	} @else if $type == monster {
		color: green;
	} @else {
		color: black;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	color: green; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;h4 id=&#39;8_2&#39;&gt;8.2. @for&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;@for&lt;/code&gt; 指令在限制的范围内重复输出一系列的样式，每次按要求（变量的值）对输出结果做出变动。这个指令包含两种格式：&lt;code&gt;@for $var from &amp;lt;start&amp;gt; through &amp;lt;end&amp;gt;&lt;/code&gt;，或者 &lt;code&gt;@for $var from &amp;lt;start&amp;gt; to &amp;lt;end&amp;gt;&lt;/code&gt;，区别在于 &lt;code&gt;through&lt;/code&gt; 与 &lt;code&gt;to&lt;/code&gt; 的含义：&lt;em&gt;当使用 &lt;code&gt;through&lt;/code&gt; 时，条件范围包含 &lt;code&gt;&amp;lt;start&amp;gt;&lt;/code&gt; 与 &lt;code&gt;&amp;lt;end&amp;gt;&lt;/code&gt; 的值，而使用 &lt;code&gt;to&lt;/code&gt; 时条件范围只包含 &lt;code&gt;&amp;lt;start&amp;gt;&lt;/code&gt; 的值不包含 &lt;code&gt;&amp;lt;end&amp;gt;&lt;/code&gt; 的值&lt;/em&gt;。另外，&lt;code&gt;$var&lt;/code&gt; 可以是任何变量，比如 &lt;code&gt;$i&lt;/code&gt;；&lt;code&gt;&amp;lt;start&amp;gt;&lt;/code&gt; 和 &lt;code&gt;&amp;lt;end&amp;gt;&lt;/code&gt; 必须是整数值。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@for $i from 1 through 3 {
	.item-#{$i} { width: 2em * $i; }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.item-1 {
	width: 2em; }
.item-2 {
	width: 4em; }
.item-3 {
	width: 6em; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;h4 id=&#39;8_3&#39;&gt;8.3. @each&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;@each&lt;/code&gt; 指令的格式是 &lt;code&gt;$var in &amp;lt;list&amp;gt;&lt;/code&gt;, &lt;code&gt;$var&lt;/code&gt; 可以是任何变量名，比如 &lt;code&gt;$length&lt;/code&gt; 或者 &lt;code&gt;$name&lt;/code&gt;，而 &lt;code&gt;&amp;lt;list&amp;gt;&lt;/code&gt; 是一连串的值，也就是值列表。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;@each&lt;/code&gt; 将变量 &lt;code&gt;$var&lt;/code&gt; 作用于值列表中的每一个项目，然后输出结果，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@each $animal in puma, sea-slug, egret, salamander {
	.#{$animal}-icon {
		background-image: url(&amp;#39;/images/#{$animal}.png&amp;#39;);
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.puma-icon {
	background-image: url(&amp;#39;/images/puma.png&amp;#39;); }
.sea-slug-icon {
	background-image: url(&amp;#39;/images/sea-slug.png&amp;#39;); }
.egret-icon {
	background-image: url(&amp;#39;/images/egret.png&amp;#39;); }
.salamander-icon {
	background-image: url(&amp;#39;/images/salamander.png&amp;#39;); }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;h4 id=&#39;8_4&#39;&gt;8.4. @while&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;@while&lt;/code&gt; 指令重复输出样式直到返回结果为 &lt;code&gt;false&lt;/code&gt;。这样可以做出比 &lt;code&gt;@for&lt;/code&gt; 指令更复杂的循环，只是很少会用到。例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$i: 6;
@while $i &amp;gt; 0 {
	.item-#{$i} { width: 2em * $i; }
	$i: $i - 2;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.item-6 {
	width: 12em; }

.item-4 {
	width: 8em; }

.item-2 {
	width: 4em; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h3 id=&#39;9&#39;&gt;9. 混合指令 (Mixin Directives)&lt;/h3&gt;
&lt;p&gt;混合指令（Mixin）用于定义可重复使用的样式，避免了使用无语意的 class，比如 &lt;code&gt;.float-left&lt;/code&gt;。混合指令可以包含所有的 CSS 规则，绝大部分 Sass 规则，甚至通过参数功能引入变量，输出多样化的样式。&lt;/p&gt;

&lt;p&gt;&lt;h4 id=&#39;9_1&#39;&gt;9.1. 定义混合指令 &lt;code&gt;@mixin&lt;/code&gt; (Defining a Mixin: &lt;code&gt;@mixin&lt;/code&gt;)&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;混合指令的用法是在 &lt;code&gt;@mixin&lt;/code&gt; 后添加名称与样式，比如名为 &lt;code&gt;large-text&lt;/code&gt; 的混合通过下面的代码定义：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin large-text {
	font: {
		family: Arial;
		size: 20px;
		weight: bold;
	}
	color: #ff0000;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;混合也需要包含选择器和属性，甚至可以用 &lt;code&gt;&amp;amp;&lt;/code&gt; 引用父选择器：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin clearfix {
	display: inline-block;
	&amp;amp;:after {
		content: &amp;quot;.&amp;quot;;
		display: block;
		height: 0;
		clear: both;
		visibility: hidden;
	}
	* html &amp;amp; { height: 1px }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;h4 id=&#39;9_2&#39;&gt;9.2. 引用混合样式 &lt;code&gt;@include&lt;/code&gt; (Including a Mixin: &lt;code&gt;@include&lt;/code&gt;)&lt;/h4&gt;&lt;/p&gt;

&lt;p&gt;使用 &lt;code&gt;@include&lt;/code&gt; 指令引用混合样式，格式是在其后添加混合名称，以及需要的参数（可选）：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.page-title {
	@include large-text;
	padding: 4px;
	margin-top: 10px;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.page-title {
	font-family: Arial;
	font-size: 20px;
	font-weight: bold;
	color: #ff0000;
	padding: 4px;
	margin-top: 10px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;也可以在最外层引用混合样式，不会直接定义属性，也不可以使用父选择器。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin silly-links {
	a {
		color: blue;
		background-color: red;
	}
}

@include silly-links;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;a {
	color: blue;
	background-color: red; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;混合样式中也可以包含其他混合样式，比如&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin compound {
	@include highlighted-background;
	@include header-text;
}

@mixin highlighted-background { background-color: #fc0; }
@mixin header-text { font-size: 20px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;混合样式中应该只定义后代选择器，这样可以安全的导入到文件的任何位置。&lt;/p&gt;
&lt;h4 id=&#39;9_3&#39;&gt;9.3. 参数 (Arguments)&lt;/h4&gt;
&lt;p&gt;参数用于给混合指令中的样式设定变量，并且赋值使用。在定义混合指令的时候，按照变量的格式，通过逗号分隔，将参数写进圆括号里。引用指令时，按照参数的顺序，再将所赋的值对应写进括号：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin sexy-border($color, $width) {
	border: {
		color: $color;
		width: $width;
		style: dashed;
	}
}

p { @include sexy-border(blue, 1in); }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	border-color: blue;
	border-width: 1in;
	border-style: dashed; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;混合指令也可以使用给变量赋值的方法给参数设定默认值，然后，当这个指令被引用的时候，如果没有给参数赋值，则自动使用默认值：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin sexy-border($color, $width: 1in) {
	border: {
		color: $color;
		width: $width;
		style: dashed;
	}
}
p { @include sexy-border(blue); }
h1 { @include sexy-border(blue, 2in); }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
	border-color: blue;
	border-width: 1in;
	border-style: dashed; }

h1 {
	border-color: blue;
	border-width: 2in;
	border-style: dashed; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h5 id=&#39;9_3_1&#39;&gt;9.3.1. 关键词参数 (Keyword Arguments)&lt;/h5&gt;
&lt;p&gt;混合指令也可以使用关键词参数，上面的例子也可以写成：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p { @include sexy-border($color: blue); }
h1 { @include sexy-border($color: blue, $width: 2in); }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;虽然不够简明，但是阅读起来会更方便。关键词参数给函数提供了更灵活的接口，以及容易调用的参数。关键词参数可以打乱顺序使用，如果使用默认值也可以省缺，另外，参数名被视为变量名，下划线、短横线可以互换使用。&lt;/p&gt;
&lt;h5 id=&#39;9_3_2&#39;&gt;9.3.2. 参数变量 (Variable Arguments)&lt;/h5&gt;
&lt;p&gt;有时，不能确定混合指令需要使用多少个参数，比如一个关于 &lt;code&gt;box-shadow&lt;/code&gt; 的混合指令不能确定有多少个 &amp;#8216;shadow&amp;#8217; 会被用到。这时，可以使用参数变量 &lt;code&gt;…&lt;/code&gt; 声明（写在参数的最后方）告诉 Sass 将这些参数视为值列表处理：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin box-shadow($shadows...) {
	-moz-box-shadow: $shadows;
	-webkit-box-shadow: $shadows;
	box-shadow: $shadows;
}

.shadows {
	@include box-shadow(0px 4px 5px #666, 2px 6px 10px #999);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.shadowed {
	-moz-box-shadow: 0px 4px 5px #666, 2px 6px 10px #999;
	-webkit-box-shadow: 0px 4px 5px #666, 2px 6px 10px #999;
	box-shadow: 0px 4px 5px #666, 2px 6px 10px #999;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;参数变量也可以用在引用混合指令的时候 (&lt;code&gt;@include&lt;/code&gt;)，与平时用法一样，将一串值列表中的值逐条作为参数引用：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin colors($text, $background, $border) {
	color: $text;
	background-color: $background;
	border-color: $border;
}

$values: #ff0000, #00ff00, #0000ff;
.primary {
	@include colors($values...);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.primary {
	color: #ff0000;
	background-color: #00ff00;
	border-color: #0000ff;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;blockquote&gt;
&lt;p&gt;You can use variable arguments to wrap a mixin and add additional styles without changing the argument signature of the mixin. If you do so, even keyword arguments will get passed through to the wrapped mixin. For example:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin wrapped-stylish-mixin($args...) {
	font-weight: bold;
	@include stylish-mixin($args...);
}

.stylish {
	// The $width argument will get passed on to &amp;quot;stylish-mixin&amp;quot; as a keyword
	@include wrapped-stylish-mixin(#00ff00, $width: 100px);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;上面注释内的意思是：&lt;code&gt;$width&lt;/code&gt; 参数将会传递给 &lt;code&gt;stylish-mixin&lt;/code&gt; 作为关键词。&lt;/p&gt;
&lt;h4 id=&#39;9_4&#39;&gt;9.4. 向混合样式中导入内容 (Passing Content Blocks to a Mixin)&lt;/h4&gt;
&lt;p&gt;在引用混合样式的时候，可以先将一段代码导入到混合指令中，然后再输出混合样式，额外导入的部分将出现在 &lt;code&gt;@content&lt;/code&gt; 标志的地方：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@mixin apply-to-ie6-only {
	* html {
		@content;
	}
}
@include apply-to-ie6-only {
	#logo {
		background-image: url(/logo.gif);
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;* html #logo {
	background-image: url(/logo.gif);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;strong&gt;为便于书写，&lt;code&gt;@mixin&lt;/code&gt; 可以用 &lt;code&gt;=&lt;/code&gt; 表示，而 &lt;code&gt;@include&lt;/code&gt; 可以用 &lt;code&gt;+&lt;/code&gt; 表示&lt;/strong&gt;，所以上面的例子可以写成：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;=apply-to-ie6-only
	* html
		@content

+apply-to-ie6-only
	#logo
		background-image: url(/logo.gif)&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;&lt;strong&gt;注意：&lt;/strong&gt; 当 &lt;code&gt;@content&lt;/code&gt; 在指令中出现过多次或者出现在循环中时，额外的代码将被导入到每一个地方。&lt;/p&gt;
&lt;h5 id=&#39;9_4_1&#39;&gt;9.4.1. Variable Scope and Content Blocks&lt;/h5&gt;
&lt;blockquote&gt;
&lt;p&gt;The block of content passed to a mixin are evaluated in the scope where the block is defined, not in the scope of the mixin. This means that variables local to the mixin cannot be used within the passed style block and variables will resolve to the global value:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$color: white;
@mixin colors($color: blue) {
	background-color: $color;
	@content;
	border-color: $color;
}
.colors {
	@include colors { color: $color; }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;编译为&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.colors {
	background-color: blue;
	color: white;
	border-color: blue;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;blockquote&gt;
&lt;p&gt;Additionally, this makes it clear that the variables and mixins that are used within the passed block are related to the other styles around where the block is defined. For example:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#sidebar {
	$sidebar-width: 300px;
	width: $sidebar-width;
	@include smartphone {
		width: $sidebar-width / 3;
	}
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h3 id=&#39;10&#39;&gt;10. 函数指令 (Function Directives)&lt;/h3&gt;
&lt;p&gt;Sass 允许自定义函数作用于任何值或者字符串，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;$grid-width: 40px;
$gutter-width: 10px;

@function grid-width($n) {
	@return $n * $grid-width + ($n - 1) * $gutter-width;
}

#sidebar { width: grid-width(5); }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;结果是&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#sidebar {
	width: 240px; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;函数可以使用任何变量，或者像混合指令一样使用参数。一个函数通常包含很多连结在一起的指令，通过 &lt;code&gt;@return&lt;/code&gt; 设定函数的返回值。&lt;/p&gt;

&lt;p&gt;自定义的函数也可以使用关键词参数，上面的例子可以这样写：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#sidebar { width: grid-width($n: 5); }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;建议在自定义函数前添加前缀，避免与 Sass 的默认函数冲突，也可以让其他人在阅读 Sass 文件时知道这是自定义函数。&lt;/p&gt;
&lt;h3 id=&#39;11&#39;&gt;11. 输出样式 (Output Style)&lt;/h3&gt;
&lt;p&gt;Sass 支持多种输出样式，满足不同的需求。通过 &lt;code&gt;:style&lt;/code&gt; 选项设定输出样式，或者在命令行中使用 &lt;code&gt;--style&lt;/code&gt; 命令。&lt;/p&gt;
&lt;h4 id=&#39;11_1&#39;&gt;11.1. :nested&lt;/h4&gt;
&lt;p&gt;Nested （嵌套）样式是默认的 Sass 输出方式，这种样式清楚地反映了 CSS 与 HTML 的结构。选择器与属性等单独占用一行，缩进量与 Sass 文件中一致，每行的缩进量反映了其在嵌套规则内的层数。当阅读比较大的 CSS 文件时，这种样式可以很容易分析文件的结构。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	color: #fff;
	background-color: #000; }
	#main p {
		width: 10em; }

.huge {
	font-size: 10em;
	font-weight: bold;
	text-decoration: underline; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;11_2&#39;&gt;11.2. :expanded&lt;/h4&gt;
&lt;p&gt;Expanded 输出方式更像是手写的，选择器、属性等各占用一行，属性根据选择器缩进，而选择器不做任何缩进。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main {
	color: #fff;
	background-color: #000;
}
#main p {
	width: 10em;
}

.huge {
	font-size: 10em;
	font-weight: bold;
	text-decoration: underline;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;11_3&#39;&gt;11.3. :compact&lt;/h4&gt;
&lt;p&gt;Compact 输出方式比起上面两种占用的空间更少，每条 CSS 规则只占一行，包含其下的所有属性。嵌套的选择器在输出时紧邻在一起，与其他组通过空白行分隔。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main { color: #fff; background-color: #000; }
#main p { width: 10em; }

.huge { font-size: 10em; font-weight: bold; text-decoration: underline; }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h4 id=&#39;11_4&#39;&gt;11.4 :compressed&lt;/h4&gt;
&lt;p&gt;Compressed 输出方式删除无意义的空格与空白行，以及注释，将文件压缩到最小，同时也会做出其他调整，比如智能换用占空间最小的颜色表达方式。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;#main{color:#fff;background-color:#000}#main p{width:10em}.huge{font-size:10em;font-weight:bold;text-decoration:underline}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;h3 id=&#39;12&#39;&gt;12. 拓展 Sass&lt;/h3&gt;
&lt;p&gt;Sass 提供了很多高级自定义功能，使用这些功能需要有良好的 Ruby 基础。&lt;/p&gt;
&lt;h4 id=&#39;12_1&#39;&gt;12.1. 自定义 Sass 函数&lt;/h4&gt;
&lt;p&gt;用户可以通过 Ruby API 自定义 Sass 函数，详细信息请查看 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/Sass/Script/Functions.html#adding_custom_functions&#39; target=&#39;_blank&#39;&gt;source documentation&lt;/a&gt; 。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;以下关于 Sass 缓存与自定义导入设置的内容暂时未翻译，请查看 &lt;a href=&#39;http://sass-lang.com/docs/yardoc/file.SASS_REFERENCE.html#cache_stores&#39; target=&#39;_blank&#39;&gt;原文档&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;!--
&lt;h4 id=&quot;12_2&quot;&gt;12.2. Cache Stores&lt;/h4&gt;
&lt;h4 id=&quot;12_3&quot;&gt;12.3 Custom Importers&lt;/h4&gt;
--&gt;&lt;div id=&#39;button&#39;&gt;&lt;a href=&#39;#contents&#39;&gt;目录&lt;/a&gt;&lt;/div&gt;</description>
    </item>
    
    <item>
      <title>【总结】DropboxDiff 对比文件的历史版本</title>
      <link>http://peters-playground.com/Mac_OS/2013/01/19/dropbox-diff.html</link>
      <pubDate>19 Jan 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Mac_OS/2013/01/19/dropbox-diff</guid>
      <description>&lt;p&gt;写 HTML, CSS, Javascript，当修改幅度比较大时，偶尔会想查看之前的版本，使用版本控制系统 (SVN) 好像有点大材小用，毕竟不是多人协作，也不是太复杂的项目。所以，我一般将文件存放在 Dropbox 下，保存文件时 Dropbox 会自动备份历史版本，需要恢复或对比的时候，就可以在 Dropbox 网站查看历史记录。但是，仅仅这样还不够方便，借助 &lt;a href=&#39;http://blog.vicshih.com/2011/09/dropboxdiff-chrome-extension.html&#39; target=&#39;_blank&#39;&gt;DropboxDiff&lt;/a&gt; 这款 Chrome 插件，可以直接对比两个文件的具体区别，做到版本控制软件那样的效果。&lt;/p&gt;

&lt;p&gt;Chrome Web Store 中搜索 &lt;code&gt;DropboxDiff&lt;/code&gt; (Extensions) 并安装，之后，查看历史版本的时候，每个条目的后面会自动添加两个单选框。选择需要对比的文件，然后按下 “Diff” 按钮就可以启用预设的软件对比两个文件。&lt;/p&gt;
&lt;a class=&#39;img&#39; href=&#39;/images/dropboxdiff/img1.png&#39; target=&#39;_blank&#39;&gt;&lt;img alt=&#39;dropboxdiff&#39; src=&#39;/images/dropboxdiff/img1.png&#39; /&gt;&lt;/a&gt;
&lt;p&gt;第一次使用时，需要先设置对比软件 (diff tool)，比如作者推荐的 &lt;a href=&#39;http://kdiff3.sourceforge.net/&#39; target=&#39;_blank&#39;&gt;kdiff3&lt;/a&gt;，Xcode 自带的 FileMerge，或者其他可用命令行操作的软件（好像不支持 Kaleidoscope ?），输入执行命令，再保存即可。&lt;/p&gt;
&lt;a class=&#39;img&#39; href=&#39;/images/dropboxdiff/img2.png&#39; target=&#39;_blank&#39;&gt;&lt;img alt=&#39;dropboxdiff setting&#39; src=&#39;/images/dropboxdiff/img2.png&#39; /&gt;&lt;/a&gt;
&lt;p&gt;FileMerge 的命令是 &lt;code&gt;opendiff file1 file2&lt;/code&gt;，如果在使用时遇到了路径错误，试试下面的解决办法：&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;引用 &lt;a href=&#39;http://stackoverflow.com/questions/9300748/opendiff-does-not-start-anymore&#39; target=&#39;_blank&#39;&gt;StackOverflow&lt;/a&gt; 上 Valexa 的解释：That is because with 4.3 /Developer/ moved to /Applications/Xcode.app/Contents/Developer/ . The proper setting for the new path is: &lt;code&gt;sudo xcode-select -switch /Applications/Xcode.app/Contents/Developer/&lt;/code&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;a class=&#39;img&#39; href=&#39;/images/dropboxdiff/img3.png&#39; target=&#39;_blank&#39;&gt;&lt;img alt=&#39;FileMerge&#39; src=&#39;/images/dropboxdiff/img3.png&#39; /&gt;&lt;/a&gt;
&lt;blockquote&gt;
&lt;p&gt;这是我的 Dropbox 推广链接 &lt;a href=&#39;http://db.tt/dKXdR3gf&#39; target=&#39;_blank&#39;&gt;http://db.tt/dKXdR3gf&lt;/a&gt;，通过这个链接注册 Dropbox 账户，我们每个人都可以获得额外的 500MB 空间 :)&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;&lt;strong&gt;PS:&lt;/strong&gt; Sublime Text 2 用户也可以试试 &lt;a href=&#39;https://github.com/vishr/local-history&#39; target=&#39;_blank&#39;&gt;Local History&lt;/a&gt; 这款插件，提供了在本地保存、管理文件的历史记录的功能。&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>【分享】Jekyll Alfred Extensions</title>
      <link>http://peters-playground.com/Mac_OS/2013/01/08/jekyll-alfred-extensions.html</link>
      <pubDate>08 Jan 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Mac_OS/2013/01/08/jekyll-alfred-extensions</guid>
      <description>&lt;p&gt;三个 Alfred Extensions 让 Jekyll 用起来更方便：Create a Jekyll Post, Generate Jekyll site, 以及 Push Jekyll post to Github。可以直接从 &lt;a href=&#39;https://github.com/P233/Jekyll-Alfred-Extensions&#39; target=&#39;_blank&#39;&gt;Github&lt;/a&gt; 下载，或者根据下面的代码自己创建。&lt;/p&gt;

&lt;h3 id=&#39;create_a_jekyll_post&#39;&gt;Create a Jekyll Post&lt;/h3&gt;

&lt;p&gt;快速创建一篇 Jekyll 文章。下载后双击安装，在 Alfred 管理面板中右键选择 “Show in Finder”，打开 new_post.rb 文件，然后添加你自己的 _posts 文件夹路径，格式必须以 &lt;code&gt;/Users/user_name/&lt;/code&gt; 开头。创建新文章时，在 Alfred 中输入 &lt;code&gt;post&lt;/code&gt; 与文件名，文件名中的空格请用 &lt;code&gt;-&lt;/code&gt; 代替。以下是 ruby 源码：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;# Please replace your own _posts folder path in the next line. The format should start with /Users/user_name/
Dir.chdir &amp;quot;/Users/lpw/Dropbox/Websites/Jekyll/_posts/&amp;quot;

today = Time.now.strftime(&amp;#39;%Y-%m-%d&amp;#39;)
input = ARGV[0]

filename = [today, input].join(&amp;#39;-&amp;#39;)
extension = &amp;#39;md&amp;#39;
	
file = [filename, extension].join(&amp;#39;.&amp;#39;)

system(%[touch &amp;quot;#{file}&amp;quot;])
system(%[open &amp;quot;#{file}&amp;quot;])&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;generate_jekyll_site&#39;&gt;Generate Jekyll site&lt;/h3&gt;

&lt;p&gt;通过 AppleScript 在 Terminal 中自动运行 &lt;code&gt;cd&lt;/code&gt; 与 &lt;code&gt;jekyll --server --auto&lt;/code&gt; 两个命令，然后在浏览器中访问网站，不要忘记替换成自己的 Jekyll 文件夹的路径。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;tell application &amp;quot;Terminal&amp;quot;
    activate
    if (count of windows) is 0 then
        set currentTab to do script &amp;quot;cd ~/dropbox/websites/jekyll/&amp;quot;
    else
        do script &amp;quot;cd ~/dropbox/websites/jekyll/&amp;quot; in window 1
    end if
    do script &amp;quot;jekyll --server --auto&amp;quot; in window 1
    delay 2
    do shell script &amp;quot;open http://0.0.0.0:4000&amp;quot;
end tell&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;push_jekyll_post_to_github&#39;&gt;Push Jekyll post to Github&lt;/h3&gt;

&lt;p&gt;通过 AppleScript 在 Terminal 中将生成的网站复制到新地址，并将新地址下的内容上传到 Github。之所以复制是因为 Github Pages 不支持插件，只能上传生成的网站，如果你不需要这个步骤，将下面的 &lt;code&gt;if&lt;/code&gt; 语句删除即可。同样需要替换成自己的 Jekyll 文件夹路径。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;on alfred_script(message)
  tell application &amp;quot;Terminal&amp;quot;
    activate

    if (count of windows) is 0 then
        set currentTab to do script &amp;quot;cp -a ~/dropbox/websites/jekyll/_site/. ~/dropbox/websites/p233.github.com/&amp;quot;
    else
        do script &amp;quot;cp -a ~/dropbox/websites/jekyll/_site/. ~/dropbox/websites/p233.github.com/&amp;quot; in window 1
    end if
    
    do script &amp;quot;cd ~/dropbox/websites/p233.github.com/&amp;quot; in window 1
    do script &amp;quot;git add .&amp;quot; in window 1
    do script &amp;quot;git commit -am &amp;#39;&amp;quot; &amp;amp; message &amp;amp; &amp;quot;&amp;#39;&amp;quot; in window 1
    do script &amp;quot;git push&amp;quot; in window 1
end tell
end alfred_script&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;</description>
    </item>
    
    <item>
      <title>【翻译】关于 Css Transition 的一切</title>
      <link>http://peters-playground.com/Css/2013/01/06/css-transitions.html</link>
      <pubDate>06 Jan 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Css/2013/01/06/css-transitions</guid>
      <description>&lt;p&gt;刚刚读到的文章，非常不错，翻译出来与大家分享。原文 &lt;a href=&#39;http://blog.alexmaccaw.com/css-transitions&#39; target=&#39;_blank&#39;&gt;All you need to know about Css Transitions&lt;/a&gt; 由 Alex Maccaw 发表于 2013 年 1 月 3 日。不想花太多时间纠结于字面翻译，只专注最核心的信息，Javascript 入门水平，因此后半部分理解不深入，详细信息还请读原文，翻译不当的地方请大家指正。&lt;/p&gt;

&lt;h3 id=&#39;id1182&#39;&gt;浏览器支持&lt;/h3&gt;

&lt;p&gt;几乎所有版本的 Firefox, Safari, Chrome 以及最新的 IE10 都支持 transition 属性。动画与渐变特效在 Safari 和 Chrome 下仍然需要添加 &lt;code&gt;-webkit&lt;/code&gt; 前缀，不过很快也会不需要了。&lt;/p&gt;

&lt;h3 id=&#39;_transition_&#39;&gt;使用 transition 属性&lt;/h3&gt;

&lt;p&gt;Transition 属性使用最多的地方要数 css 伪类了，比如 &lt;code&gt;:hover&lt;/code&gt;。使用时指定变化的属性名，变化时间，以及变化样式。例如&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.element {
  height: 100px;
  transition: height 2s linear;
}

.element:hover {
  height: 200px;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;当 &lt;code&gt;:hover&lt;/code&gt; 伪类被激活时，&lt;code&gt;.element&lt;/code&gt; 的高度在 2 秒钟的时间内从 &lt;code&gt;100px&lt;/code&gt; 线性（linear）变化为 &lt;code&gt;200px&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;浏览器默认变化属性为 &lt;code&gt;all&lt;/code&gt; （全部属性），变化样式为 &lt;code&gt;ease&lt;/code&gt;，因此如果没特殊要求，只需定义变化的时间。&lt;/p&gt;

&lt;p&gt;使用 js 添加、移除 class 要比使用伪类更方便，因为后者必须用户激活后才能触发。下面的例子里用到了两个变化效果，都是通过 js 触发的。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.element {
  opacity: 0.0;
  transform: scale(0.95) translate3d(0,100%,0);
  transition: transform 400ms ease, opacity 400ms ease;
}

.element.active {
  opacity: 1.0;
  transform: scale(1.0) translate3d(0,0,0);
}

.element.inactive {
  opacity: 0.0;
  transform: scale(1) translate3d(0,0,0);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;var active = function(){
  $(&amp;#39;.element&amp;#39;).removeClass(&amp;#39;inactive&amp;#39;).addClass(&amp;#39;active&amp;#39;);
};

var inactive = function(){
  $(&amp;#39;.element&amp;#39;).removeClass(&amp;#39;active&amp;#39;).addClass(&amp;#39;inactive&amp;#39;);
};&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;id1183&#39;&gt;渐变背景的变化&lt;/h3&gt;

&lt;p&gt;不是所有的 css 属性都可以变化，而且只能从一个绝对值变化到另一个绝对值。比如，&lt;code&gt;height&lt;/code&gt; 不能从 &lt;code&gt;0px&lt;/code&gt; 变化到 &lt;code&gt;auto&lt;/code&gt;（补充：不同单位之间也可以变化，比如从 &lt;code&gt;0px&lt;/code&gt; 变化到 &lt;code&gt;10em&lt;/code&gt;，但不可以从 &lt;code&gt;0px&lt;/code&gt; 变化到 &lt;code&gt;10%&lt;/code&gt;），可变化的属性列表请参考&lt;a href=&#39;http://oli.jp/2010/css-animatable-properties/&#39; target=&#39;_blank&#39;&gt;这里&lt;/a&gt;。&lt;/p&gt;

&lt;p&gt;还有，只有纯色背景可以变化，带有渐变效果的背景却不可以，没有任何技术原因不支持，只是浏览器还需要些时间支持这些特效。不过，还是有几种变通办法的：&lt;/p&gt;

&lt;p&gt;第一种，渐变颜色采用有透明度的 rgba，然后变化 &lt;code&gt;background-color&lt;/code&gt; 背景颜色，如下：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.panel {
  background-color: #000;
  background-image: linear-gradient(rgba(255, 255, 0, 0.4), #FAFAFA);
  transition: background-color 400ms ease;
}

.panel:hover {
  background-color: #DDD;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;如果渐变样式是连续的，还可以通过 &lt;code&gt;background-position&lt;/code&gt; 属性移动它，&lt;a href=&#39;http://sapphion.com/2011/10/css3-gradient-transition-with-background-position/&#39; target=&#39;_blank&#39;&gt;例子&lt;/a&gt;。或者，创建两个元素叠加，控制上面一层的透明度。如下：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.element {  
  width: 100px;  
  height: 100px;  
  position: relative;
  background: linear-gradient(#C7D3DC,#5B798E);    
}  

.element .inner { 
  content: &amp;#39;&amp;#39;;
  position: absolute;
  left: 0; top: 0; right: 0; bottom: 0;
  background: linear-gradient(#DDD, #FAFAFA);          
  opacity: 0;
  transition: opacity 1s linear;
}

.element:hover .inner {
  opacity: 1;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;但是，这样需要额外的标签，上一层的元素也会获取鼠标事件。所以，如果这里能用 &lt;code&gt;:before&lt;/code&gt;，&lt;code&gt;:after&lt;/code&gt; 伪类是最理想的，可惜目前只有 Firefox 支持伪类变化。Eliott Sprehn 正在努力让 webkit 也支持伪类变化，这一功能很快也会在 Safari Chrome 上实现。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;更新：&lt;/strong&gt;这个问题现在已经解决，目前 Firefox，Chrome 支持伪类变化，Safari 会在下一个版本中更新，Opera 目前还不支持，IE10 也支持，但有点奇特，要用下面的方式 (Source: &lt;a href=&#39;http://css-tricks.com/pseudo-element-animationstransitions-bug-fixed-in-webkit/&#39; target=&#39;_blank&#39;&gt;CSS-Tricks&lt;/a&gt;)&lt;/p&gt;
&lt;/blockquote&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.x:hover:before { /* By itself, doesn&amp;#39;t work */ }

.x:hover {} 
.x:hover:before { /* This works (needs :hover declared) */ }&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;id1184&#39;&gt;硬件加速&lt;/h3&gt;

&lt;p&gt;变化某些属性，比如 &lt;code&gt;left&lt;/code&gt; ，&lt;code&gt;margin&lt;/code&gt;，&lt;em&gt;浏览器会重新计算每一帧的显示效果&lt;/em&gt;，这严重影响速度，尤其是在移动设备上。解决办法就是让 GPU 来做这些运算，简单的说，就是将元素转化为图片再制作变化效果，而不是重新计算每一帧的 css 样式。最简单的一个使用硬件加速的办法就是用 &lt;code&gt;transform: translate3d(0,0,0);&lt;/code&gt; 属性。&lt;/p&gt;

&lt;p&gt;不过，这并不是个完美解决方法，同时会带来一些弊端，所以要有选择的使用硬件加速，不要用的太多。其中一项弊端是，字体在变化时会 lose weight （大家应该感觉到了，变化过程中字体很细，变化完毕后突然加粗，即便没有使用粗体），原因就是硬件加速过程中不会对字体启用抗锯齿特效，避免这个问题只能禁用 &lt;code&gt;font-smoothing: antialiased;&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;此外，不用的浏览器使用不同的硬件加速库，有浏览器差异。如果遇到不同浏览器中显示的变化效果不同，不要使用通用的 &lt;code&gt;transform3d()&lt;/code&gt; 设置，给不同浏览器不同的变化参数。&lt;/p&gt;

&lt;h3 id=&#39;clipping&#39;&gt;Clipping&lt;/h3&gt;

&lt;p&gt;为了让 GPU 更好的运算变化效果，要避免使用像 &lt;code&gt;width&lt;/code&gt; 这样需要重新计算每一帧样式的属性，而用 clipping（术语，only drawing things that will be visible to the viewer）。下面的例子将一个搜索框拉宽，使用 &lt;code&gt;translate3d&lt;/code&gt; 属性令元素在 X 轴上拉长，而不是用 &lt;code&gt;width&lt;/code&gt;，这样每一帧都不会重新计算元素的宽度，速度更快，效果更流畅：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;.clipped {
  overflow: hidden;
  position: relative;
}

.clipped .clip {
  right: 0px;
  width: 45px;
  height: 45px;
  background: url(/images/clip.png) no-repeat
}

input:focus {
  -webkit-transform: translate3d(-50px, 0, 0);
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;id1185&#39;&gt;时间函数&lt;/h3&gt;

&lt;p&gt;浏览器内置的时间函数包括 &lt;code&gt;linear, ease, ease-in, ease-out and ease-in-out&lt;/code&gt;，复杂一点，可以用 cubic-bezier 曲线自定义时间函数，比如 &lt;code&gt;transition: -webkit-transform 1s cubic-bezier(.17,.67,.69,1.33);&lt;/code&gt; 。两个帮你选择时间函数的工具 &lt;a href=&#39;http://easings.net&#39; target=&#39;_blank&#39;&gt;Pre-defined Curves&lt;/a&gt; 和 &lt;a href=&#39;http://cubic-bezier.com/#.17,.67,.83,.67&#39; target=&#39;_blank&#39;&gt;Graphing Tool&lt;/a&gt;。&lt;/p&gt;

&lt;h3 id=&#39;id1186&#39;&gt;用程序控制变化效果&lt;/h3&gt;

&lt;p&gt;借助 js 可以更有效的控制变化的效果，例如将连续的变化做成动画。变化属性为 &lt;code&gt;all&lt;/code&gt; 时意味着元素的所有属性都可以同时发生变化，请看下面的例子&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;var defaults = {
  duration: 400,
  easing: &amp;#39;&amp;#39;
};

$.fn.transition = function (properties, options) {
  options = $.extend({}, defaults, options);
  properties[&amp;#39;webkitTransition&amp;#39;] = &amp;#39;all &amp;#39; + options.duration + &amp;#39;ms &amp;#39; + options.easing;
  $(this).css(properties);
};&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;然后用 jQuery 函数 &lt;code&gt;$.fn.transition&lt;/code&gt; 调用变化样式&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;$(&amp;#39;.element&amp;#39;).transition({background: &amp;#39;red&amp;#39;});&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;_callback&#39;&gt;获取变化结束的 callback&lt;/h3&gt;

&lt;p&gt;组合多个 transitions 首先要知道变化在什么时间结束，webkit 浏览器中可以监视 &lt;code&gt;webkitTransitionEnd&lt;/code&gt; 事件，其他浏览器需要先用 &lt;a href=&#39;https://gist.github.com/4414792&#39; target=&#39;_blank&#39;&gt;这个&lt;/a&gt; 找出事件名称。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;var callback = function () {
  // ...
}

$(this).one(&amp;#39;webkitTransitionEnd&amp;#39;, callback)
$(this).css(properties);&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;有时也许不工作，比如属性没有变化，或者没有被激活，确保每次都能得到一个 callback，要设定 timeout 手动激活事件。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;$.fn.emulateTransitionEnd = function(duration) {
  var called = false, $el = this;
  $(this).one(&amp;#39;webkitTransitionEnd&amp;#39;, function() { called = true; });
  var callback = function() { if (!called) $($el).trigger(&amp;#39;webkitTransitionEnd&amp;#39;); };
  setTimeout(callback, duration);
};&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;现在可以调用 &lt;code&gt;$.fn.emulateTransitionEnd()&lt;/code&gt; 确保变化结束的 callback 被激活。&lt;/p&gt;

&lt;h3 id=&#39;id1187&#39;&gt;连续的变化&lt;/h3&gt;

&lt;p&gt;熟悉用 js 调用变化，与获取变化结束的 callback 后，可以进一步将变化进行排列，做成连续的动画。jQuery 提供了两个对列相关的函数 &lt;code&gt;$.fn.queue(callback)&lt;/code&gt; 与 &lt;code&gt;$.fn.dequeue()&lt;/code&gt;。先将 css 变化样式写入 &lt;code&gt;$.fn.queue&lt;/code&gt; callback 中，变化结束后调用 &lt;code&gt;$.fn.dequeue&lt;/code&gt;。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;var $el = $(this);
$el.queue(function(){
  $el.one(&amp;#39;webkitTransitionEnd&amp;#39;, function(){
    $el.dequeue();
  });
  $el.css(properties);
});&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;这个例子很简单，再复杂一点，用 &lt;code&gt;delay()&lt;/code&gt; 函数做成一个连续在一起的动画效果。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;$(&amp;#39;.element&amp;#39;).transition({left: &amp;#39;20px&amp;#39;})
             .delay(200)
             .transition({background: &amp;#39;red&amp;#39;});&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;id1188&#39;&gt;重绘&lt;/h3&gt;

&lt;p&gt;在使用 &lt;code&gt;transition&lt;/code&gt; 属性时，要写两套 css 属性，初始样式与结束样式。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;$(&amp;#39;.element&amp;#39;).css({left: &amp;#39;10px&amp;#39;})
             .transition({left: &amp;#39;20px&amp;#39;});&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;然而，如果同时设定了两套样式，紧连在一起，浏览器试着完善属性之间的变化，却会忽略初始样式并放弃变化。在后台，浏览器其实是分批更新变化样式的，这样会加快速度，但有时也有不利的影响（注，这一段没有经验，不保证翻译的准确度）。&lt;/p&gt;

&lt;p&gt;解决办法是利用 DOM 元素的 &lt;code&gt;offsetHeight&lt;/code&gt; 属性让浏览器在两套样式之间重绘元素，例如：&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;$.fn.redraw = function(){
  $(this).each(function(){
    var redraw = this.offsetHeight;
  });
};&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;这个方法在大多数浏览器下都工作正常，但在 Android 下还不够，使用 timeout 或者激活一个 class。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;$(&amp;#39;.element&amp;#39;).css({left: &amp;#39;10px&amp;#39;})
             .redraw()
             .transition({left: &amp;#39;20px&amp;#39;});&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;transition_&#39;&gt;Transition 的未来&lt;/h3&gt;

&lt;p&gt;W3C 的 &lt;a href=&#39;https://dvcs.w3.org/hg/FXTF/raw-file/tip/web-anim/index.html&#39; target=&#39;_blank&#39;&gt;新草案&lt;/a&gt; 包含新的 Javascript API 应对现在的局限性，给开发者更灵活的空间。根据新的 API，可以同步动画，自定义时间函数，获取 callbacks，实在是令人期待的新功能。&lt;/p&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-javascript&#39;&gt;var anim = new Animation(elem, { left: &amp;#39;100px&amp;#39; }, 3);
anim.play();&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;p&gt;另外，可以在 &lt;a href=&#39;https://github.com/web-animations/web-animations-js&#39; target=&#39;_blank&#39;&gt;这里&lt;/a&gt; 找到一些实现新 API 的变通法子。&lt;/p&gt;

&lt;h3 id=&#39;id1189&#39;&gt;结尾&lt;/h3&gt;

&lt;p&gt;目前为止，希望你已经深入的了解了 css 的 transition 属性，以及如何组合简单的 API 制作丰富的动画效果。文中大多数 js 例子来源于 &lt;a href=&#39;https://github.com/maccman/gfx&#39; target=&#39;_blank&#39;&gt;GFX&lt;/a&gt;，这也是本文作者 Alex 写的一个 jQuery Css transition 库。&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>【翻译】关注 Css 性能 (一)</title>
      <link>http://peters-playground.com/Css/2013/01/05/css-performance.html</link>
      <pubDate>05 Jan 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Css/2013/01/05/css-performance</guid>
      <description>&lt;p&gt;虽然我只会写最简单的页面，但本着“取法乎上，得乎其中”的原则，一直关注 Css 性能，读过几篇相关文章，准备逐步翻译出来当做总结与积累。第一篇 &lt;a href=&#39;http://boagworld.com/dev/why-you-should-care-about-css-page-performance/&#39; target=&#39;_blank&#39;&gt;Why you should care about CSS page performance&lt;/a&gt; 由 Doug Stewart 发表于 2012 年 5 月。只记录下几点重要的信息，没有直接翻译，理解不当的地方还请大家指正。&lt;/p&gt;

&lt;p&gt;Doug 介绍了自己测试 css 性能的两个方法：缩放浏览器窗口与上下滚动页面。浏览器重绘页面可以反映很多关于性能的问题，未添加 css 的页面反应速度肯定是最快的。一般来说，&lt;em&gt;如果页面缩放时有明显延时，很可能是布局问题，比如浮动、流体宽度等等，而如果页面滚动时有明显延时，则可能是特效问题，比如说阴影与渐变。&lt;/em&gt;&lt;/p&gt;

&lt;p&gt;优化 css 性能，自然要先找到问题出在哪里。作者介绍了三款衡量页面性能的工具：&lt;a href=&#39;https://github.com/andyedinborough/stress-css&#39; target=&#39;_blank&#39;&gt;Css Stress Test&lt;/a&gt;，Chrome Developer Tools 的 Profiles 与 Timeline。就不一一介绍这些工具了，原则上是要找出最耗时间的地方，然后将其优化。&lt;/p&gt;

&lt;h6 id=&#39;_css_&#39;&gt;几点关于 Css 性能的小提示&lt;/h6&gt;

&lt;ol&gt;
&lt;li&gt;大家都知道 id 选择器的表现效果要优于 class，事实虽然也如此，但仅仅是非常微小的差别，几乎可以不予考虑；&lt;/li&gt;

&lt;li&gt;Css3 属性往往是影响页面性能的罪魁祸首，尤其是 &lt;code&gt;animation&lt;/code&gt; 与 &lt;code&gt;@font-face&lt;/code&gt;，谨慎地使用；&lt;/li&gt;

&lt;li&gt;现代浏览器支持 Javascript 与 Flash 硬件加速，却不支持 Css，webkit 有优化功能，但还是受限的；&lt;/li&gt;

&lt;li&gt;移动平台的情况是，即便现在最先进的手机也不及 10 年前最先进的电脑;&lt;/li&gt;

&lt;li&gt;人脑有大约 80 毫秒的反应时间，优化页面性能时不要在一些细枝末节的地方花太多精力，专注于拖慢严重的地方。&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;&lt;strong&gt;PS:&lt;/strong&gt; 留言回复 Javascript 其实没有硬件加速，Safari mobile 使用 GPU 运算 animations 与 3D 特效。&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>【总结】CSS 判断浏览器方法整理</title>
      <link>http://peters-playground.com/Css/2013/01/04/Browser-Specific-Hacks.html</link>
      <pubDate>04 Jan 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Css/2013/01/04/Browser-Specific-Hacks</guid>
      <description>&lt;p&gt;整理几个常用的判断浏览器的方法，记录在这里方便查阅。不算全面，以后遇到问题了再来更新。&lt;/p&gt;

&lt;h3 id=&#39;conditional_comments&#39;&gt;Conditional Comments&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-markup&#39;&gt;&amp;lt;!--[if IE]&amp;gt;
  According to the conditional comment this is IE
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if IE 6]&amp;gt;
  According to the conditional comment this is IE 6
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if IE 7]&amp;gt;
  According to the conditional comment this is IE 7
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if IE 8]&amp;gt;
  According to the conditional comment this is IE 8
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if IE 9]&amp;gt;
  According to the conditional comment this is IE 9
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if gte IE 8]&amp;gt;
  According to the conditional comment this is IE 8 or higher
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if lt IE 9]&amp;gt;
  According to the conditional comment this is IE lower than 9
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if lte IE 7]&amp;gt;
  According to the conditional comment this is IE lower or equal to 7
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if gt IE 6]&amp;gt;
  According to the conditional comment this is IE greater than 6
&amp;lt;![endif]--&amp;gt;

&amp;lt;!--[if !IE]&amp;gt; --&amp;gt;
  According to the conditional comment this is not IE
&amp;lt;!-- &amp;lt;![endif]--&amp;gt;&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h6 id=&#39;source_&#39;&gt;Source: &lt;a href=&#39;http://www.quirksmode.org/css/condcom.html&#39; target=&#39;_blank&#39;&gt;http://www.quirksmode.org/css/condcom.html&lt;/a&gt;&lt;/h6&gt;

&lt;h3 id=&#39;target_ie&#39;&gt;Target IE&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;p {
  color: red; /* All browsers */
  color: red\9; /* IE8 and below */
  *color: red; /* IE7 and below */
  _color: red; /* IE6 */  
}

/* Target IE 10 */
@media screen and (-ms-high-contrast: active), (-ms-high-contrast: none) {
  p {
    color: red;
  }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;target_safari_and_chrome&#39;&gt;Target Safari and Chrome&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@media screen and (-webkit-min-device-pixel-ratio:0) {
  p {
    color: red;
  }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;target_firefox&#39;&gt;Target Firefox&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;@-moz-document url-prefix() {
  p {
    color: red;
  }
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;h3 id=&#39;target_opera&#39;&gt;Target Opera&lt;/h3&gt;
&lt;div&gt;
  &lt;pre data-line=&#39;&#39;&gt;&lt;code class=&#39;language-css&#39;&gt;x:-o-prefocus, p {
  color: red;
}&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;</description>
    </item>
    
    <item>
      <title>【总结】用 Dropbox 同步 Mac 软件设置</title>
      <link>http://peters-playground.com/Mac_OS/2013/01/03/sync-application-preferences.html</link>
      <pubDate>03 Jan 2013</pubDate>
      <author>Peter</author>
      <guid>http://peters-playground.com/Mac_OS/2013/01/03/sync-application-preferences</guid>
      <description>&lt;p&gt;常常在台式机与笔记本电脑之间切换着工作，虽然 Dropbox 同步文件很方便，但偶尔也会想如果可以同步软件设置就更完美了。某个周末花了些时间搜集、整理了一些方法，在此分享出来。不必非用 Dropbox，Google Drive 等类似服务应该也可以。&lt;/p&gt;

&lt;p&gt;同步的原理是将软件的设置文件移动到 Dropbox 文件夹下，交给 Dropbox 管理，然后在存储软件设置的地方创建 symlink 指向 Dropbox 内的设置文件，软件每次读取/保存设置文件的时候其实是在 Dropbox 文件夹内操作（symlink 被称为符号链接，相当于快捷方式，具体解释还请搜索 google）。创建 symlink 的命令是: &lt;code&gt;ln -s &amp;lt;source_file&amp;gt; &amp;lt;myfile&amp;gt;&lt;/code&gt;，或者先用 &lt;code&gt;cd&lt;/code&gt; 命令打开目标地址，然后用 &lt;code&gt;ln -s &amp;lt;source_file&amp;gt;&lt;/code&gt; 命令将目标文件链接到目标地址。 &lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;如果不想使用命令，也可以试试 &lt;a href=&#39;http://joeworkman.net/blog/post-9591023714&#39; target=&#39;_blank&#39;&gt;DropboxAppSync&lt;/a&gt; 这款软件，和上面介绍的原理是一样的&lt;/p&gt;
&lt;/blockquote&gt;

&lt;h3 id=&#39;_sublime_text_2&#39;&gt;同步 Sublime Text 2&lt;/h3&gt;

&lt;p&gt;Sublime Text 2 所有的设置文件、插件都存放在 Packages 文件夹下，这是一项非常重要的设定，因为 Mac OS, Windows, Linux 三个平台之间因此可以相互同步，并且包括所有的插件！以下仅以 Mac OS 做介绍。&lt;/p&gt;

&lt;ol&gt;
&lt;li&gt;关闭 Sublime Text 2；&lt;/li&gt;

&lt;li&gt;在要同步的主机上找到 Sublime Text 2 的 packages 文件夹（路径：&lt;code&gt;~/Library/Application Support/Sublime Text 2/&lt;/code&gt;），将其移动到 Dropbox 文件夹内，例如 &lt;code&gt;~/Dropbox/Sync/Packages&lt;/code&gt;；&lt;/li&gt;

&lt;li&gt;启动 Terminal，输入 &lt;code&gt;cd ~/Library/Application\ Support/Sublime\ Text\ 2/&lt;/code&gt; 打开目标文件夹（空格前要加 &lt;code&gt;\&lt;/code&gt;），然后输入 &lt;code&gt;ln -s ~/Dropbox/Sync/Packages&lt;/code&gt; 创建一个 symlink 将 Dropbox 下的 Packages 文件夹链接到当前目录，Windows 下创建 symlink 的命令请看&lt;a href=&#39;http://technet.microsoft.com/en-us/library/cc753194(v=ws.10).aspx&#39; target=&#39;_blank&#39;&gt;这里&lt;/a&gt;；&lt;/li&gt;

&lt;li&gt;在第二台机器上，关闭 Sublime Text 2，找到 packages 文件夹并将其删除，然后重复步骤 3 中的命令。&lt;/li&gt;
&lt;/ol&gt;

&lt;h3 id=&#39;id1181&#39;&gt;同步其他软件&lt;/h3&gt;

&lt;p&gt;Mac OS 下软件的设置文件都会以 .plist 的格式保存在 &lt;code&gt;~/Library/Preferences/&lt;/code&gt; 下，只要给这些文件创建 symlink 就可以完成同步。有些软件除了 .plist 外，还会在 &lt;code&gt;~/Libray/Application Support/&lt;/code&gt; 下创建文件夹存储额外的设置，比如 Alfred 会创建一个文件夹存储 extensions，TextMate 会创建一个文件夹存储 Bundles 与 Themes，给这些文件夹创建 symlink 可以完成更彻底的同步。&lt;em&gt;但是，并不是所有的软件都允许同步这些文件夹&lt;/em&gt;，有些软件会删除 symlink 重新写入文件，而不是按照 symlink 找到位于 Dropbox 下的文件。已经同步的软件如果更新，则不会影响到 symlink。&lt;/p&gt;

&lt;h3 id=&#39;_mac_os_keychain&#39;&gt;同步 Mac OS Keychain&lt;/h3&gt;

&lt;p&gt;除了软件设置外，Mac OS 还有一样可以同步，就是 Keychain。如果你的两台 Mac 之间经常共用一套密码，同步 Keychain 会是一个很不错的选择，即便不是，把 Keychain 存储到 Dropbox 重装新系统也会保留这些密码。此外，FTP 软件普遍将登陆信息存储进 Keychain，只同步 .plist 文件是没有效果的，一定要同步 Keychain。&lt;/p&gt;

&lt;p&gt;同步方法：将 &lt;code&gt;~/Library/Keychains/&lt;/code&gt; 下的 login.keychain 文件移动到 Dropbox 文件夹下；启动 Keychain Access，选择 File &amp;gt; Add Keychain…，然后添加位于 Dropbox 下的 login.keychain 文件；在第二台电脑上删除本地的 login.keychain 文件，然后重复 File &amp;gt; Add Keychain…。同步 Keychain 后，重启电脑偶尔会被要求输入密码，验证 Keychain 的管理权限。&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;这是我的 Dropbox 推广链接 &lt;a href=&#39;http://db.tt/dKXdR3gf&#39; target=&#39;_blank&#39;&gt;http://db.tt/dKXdR3gf&lt;/a&gt;，通过这个链接注册 Dropbox 账户，我们每个人都可以获得额外的 500MB 空间 :)&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;&lt;strong&gt;最后，不要在两台电脑上同时运行同步的软件，会因为文件冲而突丢失设置。&lt;/strong&gt;&lt;/p&gt;</description>
    </item>
    

  </channel> 
</rss>